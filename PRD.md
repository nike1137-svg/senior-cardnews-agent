# PRD — 시니어 생활정보 카드뉴스 제작 에이전트

- 작성일: 2026-09-09
- 상태: **구현 완료** (2026-09-10). 실물과 어긋난 곳은 발견할 때마다 이 문서를 고쳤다
- 관련 문서: [결정 이력](DECISIONS.md) · [캡처 체크리스트](docs/screenshots/README.md)
- 과제 안내 원문은 저작권 때문에 이 저장소에 두지 않는다 (저장소 밖에 별도 보관)

---

## 1. 문제 정의

### 어떤 반복 업무인가

경로당·복지관·시니어 대상 채널에 **주기적으로 생활정보 안내물을 만들어 올리는 일**.
지금은 사람이 매번 다음을 반복한다.

1. 이번 주에 알릴 만한 소식을 여기저기서 찾는다 (날씨, 건강, 복지 공고)
2. 시니어가 이해할 수 있는 말로 다시 쓴다
3. 큰 글씨로 이미지를 만든다
4. 발송한다

찾는 데 오래 걸리고, **매번 품질이 들쭉날쭉**하며, 출처를 남기지 않아 나중에 확인이 안 된다.

### 입력 → 결과물

| 구분 | 내용 |
|---|---|
| **입력** | 주제 한 줄 (예: "이번 주 건강·생활 정보") + 대상 지역 |
| **결과물** | 카드뉴스 이미지 **5장 (PNG, 1080×1350)** + 전체 ZIP + **출처 기록 파일** |
| **선택 출력** | 승인 시 LINE 발송 (기본값은 발송하지 않음) |

### 이 에이전트가 끝내주는 것

"주제 한 줄" → "**바로 배포할 수 있는 카드뉴스 묶음**" 까지. 사람은 **고르고 승인만** 한다.

---

## 2. 타겟 유저

| 구분 | 대상 | 특징 |
|---|---|---|
| **직접 사용자** | 시니어 대상 채널 운영자 (경로당 담당자, 복지관 직원, 챗봇 운영자) | 디자인 도구를 다루지 못함. 짧은 시간에 만들어야 함 |
| **최종 수신자** | 60~80대 시니어 | 작은 글씨를 못 읽음. 외래어·전문용어에 막힘. 링크를 잘 누르지 않음 |

최종 수신자 특성이 **카드 디자인 제약**으로 직결된다.

- 본문 글자 크기 하한 **38px**, 배경 대비 명암비 **4.5:1**. 둘 다 코드가 검사한다
- 한 카드에 메시지 **하나만**
- 외래어는 우리말로 풀어 쓴다
- 숫자·날짜는 반드시 명시 ("다음 주" ❌ → "9월 15일 월요일" ⭕)

---

## 3. 워크플로 설계

### 단계별 — 에이전트가 판단할 일 / 사람이 처리할 일

| # | 단계 | 에이전트가 판단하는 것 | 사람이 하는 것 |
|---|---|---|---|
| 1 | **조사** | 검색어를 정하고 최근 소식 후보를 모은다. **후보를 고르기 전에 과거 발행 이력을 조회**해 최근에 다룬 주제는 후순위로 내린다. 검색은 3회까지 | — |
| 2 | **후보 선택** | — | **소식 1~3개 선택** ⭐ |
| 3 | **심층 검증** | `fetch_article` 로 **원문을 직접 열어** 날짜·수치 대조. **확인된 사실 / 발표자 주장 / 미확인** 으로 분류 | — |
| 4 | **스토리보드** | 독자 규칙과 카드 양식을 조회한 뒤 카드 5장의 역할·문구·자세를 계획 | **검토·승인** ⭐ |
| 5 | **카드 합성** | 배경 위에 텍스트 레이어를 얹고 **가독성을 코드로 검사**(38px 하한 · 명암비 4.5:1) | — |
| 6 | **검수** | **검토 에이전트**가 문구를 검사해 반려하면 스토리보드부터 재작업 (반려 2회 상한) | **승인 / 수정 지시** ⭐ |
| 7 | **발송** | 기본은 보내지 않는다(dry-run). 발송 뒤 발행 이력을 기록해 다음 실행이 참고하게 한다 | **발송 승인** ⭐ |

⭐ = 사람 개입 지점 (4곳). 코드의 `PHASES` (`app/agent/state.py`) 와 같은 순서·개수다.

> **계획은 9단계였다.** 구현하면서 둘로 줄었다.
> - **이미지 생성 + 카드 합성 → 카드 합성 하나로.** 배경을 실행마다 생성하지 않게 되어
>   (D-014) 별도 단계로 둘 이유가 없어졌다
> - **내보내기는 단계가 아니라 화면 동작이다.** PNG·ZIP·출처기록은 결과 화면에서
>   내려받는 것이고, 에이전트가 판단할 일이 없다. 단계로 두면 루프가 의미 없이 한 번 더 돈다

### 루프 구조

에이전트는 각 단계 종료 시 **결과를 관찰하고 다음 행동을 결정**한다.

```
계획 → 도구 호출 → 결과 관찰 → 판단
                                 ├ 충분하다      → 다음 단계
                                 ├ 부족하다      → 같은 단계 재시도 (조건 바꿔서)
                                 ├ 사람이 정해야 → status=waiting_for_user
                                 └ 불가능하다    → 대체 경로 또는 중단
```

### 종료 조건 (무한 루프 방지)

| 항목 | 상한 | 넘으면 |
|---|---|---|
| 단계별 재시도 | **3회** | 해당 단계 실패 처리 후 사람에게 보고 |
| 전체 루프 반복 | **45회** | 중단, 진행분 보존. 20 → 30 → 45 로 두 번 실측해 올렸다 (D-020 · D-024) |
| 전체 실행 시간 | **10분** | 중단, 진행분 보존 |
| 이미지 생성 호출 | **1회 실행당 8회** | 대체 경로(단색 배경)로 전환 |
| LLM 호출 (Gemini 경로) | **1회 실행당 40회 · 일일 300회** | 중단 (무료 티어라 과금은 없음) |
| 🔴 LLM 비용 (OpenAI 경로) | **1회 실행당 $0.30 · 누적 $3.00** | 즉시 중단. 기관 지급 크레딧이므로 초과 과금을 앱이 먼저 막는다 |
| 검토 에이전트 반려 | **2회** | 검토자가 계속 반려해 루프가 안 끝나는 것을 막는다. 상한 도달 시 사람에게 넘김 |
| 단계별 질문 | **3회** | 질문도 루프를 태운다. 같은 것을 세 번 물어 예산을 소진한 적이 있다 (D-023) |

---

## 4. 두뇌(LLM)와 도구 계획

### 두뇌 — LLM 어댑터 (교체 가능)

**어느 모델이 이 업무에 맞는지는 재봐야 안다.** 하나로 고정하지 않고
제공자를 어댑터로 감싸 **설정 한 줄로 교체 가능**하게 만든다.
무료 한도에 걸렸을 때 갈아탈 길이 필요하다는 실무적 이유도 있다.

| 역할 | 모델 | 근거 |
|---|---|---|
| **기본** | **`gemini-3.5-flash`** | 무료 티어. 계획 시점에 적었던 2.5 Flash 는 실제로 쓸 수 없었다 (D-014) |
| **폴백 사슬** | `gemini-3.5-flash-lite` → `gemini-3-flash-preview` → … | 무료 티어는 **모델당 하루 20회**라 한 모델로는 못 버틴다. 429·503 에 다음 모델로 갈아탄다 (D-016) |
| **비교·시연** | `gpt-5-mini` | 기관 지급 크레딧으로 연결해 **제공자 간 비교까지 마쳤다.** 실측 사용액 약 $0.05 (`EVAL.md` 2장) |

**필수 조건: 함수 호출(function calling)을 지원하는 API여야 한다.**
D-007에서 루프를 직접 구현하기로 했으므로, 내 앱이 도구를 정의하고 모델이 그중에서 고른다.
`claude -p` · `opencode run` 같은 **CLI 위임 방식은 쓰지 않는다** — CLI 자체가 에이전트여서
**내가 정의한 도구 스키마와 실패 처리 규칙이 사라진다.**
어떤 도구를 왜 골랐는지도 내 기록이 아니라 그 CLI 의 기록이 된다.

**왜 개발을 무료 모델로 하나** — $5는 개발 예산이 아니라 시연 예산이다.
평가 세트만 해도 시나리오 5개 × 세팅 3개 = 15회이고, 개발 중 실패한 실행도 토큰을 먹는다.
GPT-5(정품)로 개발하면 약 7회에 소진된다.

**🔒 9번 규칙** — Gemini 무료 티어는 입력이 학습에 쓰일 수 있다. 프롬프트에 **절대 넣지 않는다**:
LINE 채널 토큰, `.env` 내용, 온담이·이음이 자격증명. 이 프로젝트가 다루는 것은 **공개 뉴스·날씨**뿐이다.
도구 호출 결과를 프롬프트에 담을 때 키가 섞이지 않는지 확인하는 필터를 둔다.

### 연결할 도구

| # | 도구 | 용도 | 입력 | 출력 | 권한 | 위험도 |
|---|---|---|---|---|---|---|
| 1 | **웹 검색** (Tavily) | 최근 소식 조사 | 검색어, 기간 | 제목·링크·게시일 목록 | 읽기 전용, **무료 1,000크레딧/월·카드 불필요** | 🟢 |
| 2 | **원문 조회** (httpx + trafilatura) | 링크 열어 날짜·수치 대조 | URL | 본문 텍스트 | 읽기 전용, **로컬·완전 무료** | 🟢 |
| 3 | **날씨 조회** (Open-Meteo) | 지역 날씨·주의사항 | 위경도, 날짜 | 기온·강수·특보 | 읽기 전용, **키 불필요** | 🟢 |
| 4 | **이미지 생성** (Antigravity CLI) | **캐릭터 자산 제작(4자세).** 카드 배경은 코드로 그려 실행마다 부르지 않는다 (D-014) | 프롬프트 | PNG 1024×1024 | 파일 쓰기 (지정 폴더만) | 🟡 |
| 5 | **카드 합성** (로컬, Pillow) | 배경 + 텍스트 레이어 | 배경 PNG, 문구 | PNG 1080×1350 | 파일 쓰기 (지정 폴더만) | 🟢 |
| 6 | **LINE 발송** | 최종 배포 | 이미지, 대상 | 발송 결과 | **기본 dry-run**, 실발송은 승인 필요 | 🔴 |

> 여섯 개 중 **1·3·4·6은 외부 시스템** 호출이라 실패를 전제로 설계해야 한다.

### 자체 MCP 서버로 노출할 도구 (확장 3번)

*"내 도메인의 시스템을 에이전트의 도구로 노출"* — 이 프로젝트에서 그 시스템은 **안내 운영 데이터**다.
별도 MCP 서버(`senior-cardnews-mcp`)를 만들어 아래를 도구로 제공한다.

| # | 도구 | 하는 일 | 권한 |
|---|---|---|---|
| 7 | `list_past_publications` | 과거 발행 이력 조회 | 읽기 |
| 8 | `get_audience_profile` | 시니어 독자 특성 (금지 표현, 글자 크기 하한, 관심 주제) | 읽기 |
| 9 | `get_card_template` | 카드 양식 (색·레이아웃·문구 규칙) | 읽기 |
| 10 | `record_publication` | 발행 결과 기록 | 쓰기 (지정 저장소만) |

**단순 가점이 아니라 루프를 강화한다.** `list_past_publications` 덕분에 에이전트가 이렇게 판단할 수 있다.

```
후보 12개 수집 → 과거 이력 조회 → "독감 예방접종은 3주 전에 이미 다룸"
              → 판단: 후순위로 내리고 사람에게 알림
```

도구를 하나 더 붙인 것이 아니라, **에이전트가 판단할 근거를 하나 더 준 것**이다.
`record_publication` 이 다음 실행의 입력이 되므로 **사이클**이 형성된다.

> 배포 시 프로세스가 늘어나는 것을 피하려면 웹앱과 **같은 호스트에서 stdio로** 띄운다.

### 권한 최소화 원칙

- **읽기 도구와 쓰기 도구를 분리**한다. 조사 단계는 쓰기 권한을 갖지 않는다
- 파일 쓰기는 `output/<실행ID>/` **한 폴더 안으로만** 제한한다
- **Antigravity CLI에 `--dangerously-skip-permissions` 를 쓰지 않는다.** 필요한 도구만 개별 허용
- **LINE 발송은 기본값이 `dry-run`** 이다. 실제 발송은 ①사람 승인 ②환경변수 스위치, 두 조건이 모두 충족될 때만
- API 키는 전부 환경변수. 저장소에 커밋하지 않는다 (`.env` 는 `.gitignore`)

### 도구 실패 처리 규칙

| 도구 | 실패 상황 | 처리 |
|---|---|---|
| 웹 검색 | 결과 0건 | 검색어를 바꿔 **2회 재시도** → 그래도 없으면 "기간을 넓힐까요?" **사람에게 질문** |
| 원문 조회 | 페이지 열기 실패 | 해당 항목을 **"미확인"으로 분류**하고 계속 진행 (조용히 버리지 않음) |
| 날씨 조회 | 응답 없음 | 날씨 카드를 **건너뛰고** 나머지로 진행, 로그에 기록 |
| **이미지 생성** | **한도 초과 / 권한 거부** | ①1회 재시도 → ②**단색·패턴 배경으로 대체** → ③카드는 반드시 완성. 대체했음을 화면에 표시 |
| 카드 합성 | 글자가 배경에 묻힘 | 텍스트 뒤에 반투명 판을 깔고 재합성 |
| LINE 발송 | 발송 실패 | **재시도하지 않는다** (중복 발송 위험). 실패를 표시하고 사람이 다시 누르게 함 |

> ⚠️ **"종료 코드 0" 을 성공으로 보지 않는다.** 이미지 도구는 **파일이 실제로 생성되고 열리는지**를 확인한 뒤에만 성공 처리한다.

---

## 5. 사람 개입 지점

| # | 지점 | 왜 사람이 필요한가 | 되돌릴 수 있나 |
|---|---|---|---|
| 1 | 소식 선택 (2단계) | 무엇이 우리 시니어에게 의미 있는지는 사람이 안다 | ⭕ |
| 2 | 스토리보드 승인 (4단계) | 이미지 생성 **전에** 잡아야 한도 낭비가 없다 | ⭕ |
| 3 | 카드 검수 (6단계) | 오탈자·가독성은 사람 눈이 필요 | ⭕ |
| 4 | **발송 승인 (7단계)** | **발송은 취소가 불가능하다** | ❌ |

### 질문·재개 규칙

에이전트가 질문을 만들면 상태를 `waiting_for_user` 로 저장하고 화면에 질문과 선택지를 띄운다.
답변이 오면 `running` 으로 바꾸고 **저장된 세션 ID로 이어간다.**

```json
{
  "status": "need_input",
  "question_id": "audience-001",
  "version": 1,
  "question": "어떤 내용을 중심으로 담을까요?",
  "options": ["건강 정보", "복지 혜택", "날씨·생활 주의사항"],
  "multi_select": false
}
```

반드시 지킬 것:

| 상황 | 기대 동작 |
|---|---|
| 질문 상태에서 새로고침 | 같은 질문 유지, 답변 전엔 진행 안 함 |
| 같은 답변 2번 제출 | **한 번만** 실행 (`question_id` + `version` 으로 중복 차단) |
| 지난 질문에 뒤늦게 답변 | "지난 질문입니다" 안내, 새 작업 시작 안 함 |
| 실패 후 재시도 | 실패 원인 표시, **성공한 카드는 보존** |
| 서버 재시작 후 접속 | 저장된 상태 복원, 중단 지점부터 재시도 가능 |

---

## 6. 핵심 기능(MVP)과 화면 구성

### MVP 기능 목록

- [x] 주제 입력 → 조사 → 후보 목록 제시
- [x] 후보 선택 (1~3개)
- [x] 심층 검증 및 사실/주장/미확인 분류 표시
- [x] 스토리보드 생성 및 승인
- [x] 카드 이미지 생성 (배경) + 텍스트 레이어 합성
- [~] **카드별 부분 재생성은 만들지 않았다.** 검토 에이전트가 반려하면 전체를 다시 합성한다.
  텍스트가 별도 레이어라 배경 이미지를 다시 만들지는 않아 비용은 들지 않지만,
  *"승인된 카드는 유지"* 는 구현되지 않았다. 재합성 횟수는 `trace.json` 의 `regenerations` 로 센다
- [x] PNG / ZIP / 출처기록 내보내기
- [x] **실행 로그 화면 표시** (도구명·이유·결과·소요시간)
- [x] **상태 저장 및 재개** (서버 재시작 견딤)
- [x] LINE 발송 (dry-run 기본 · 실채널 확인 D-019)

### 화면 구성

| 화면 | 담는 것 |
|---|---|
| **1. 시작** | 주제 입력, 지역 선택, 시작 버튼 |
| **2. 진행** | 현재 단계 / **질문 카드**(선택지+답변) / **실행 로그 패널**(실시간) |
| **3. 검수** | 카드 미리보기(모바일 크기), 승인 또는 수정 지시 |
| **4. 결과** | PNG·ZIP 다운로드, 출처 기록, 발송 버튼(경고 문구 포함) |
| **5. 실행 기록** | 지난 실행 목록, 단계별 소요시간·토큰·비용, 실패 라벨 분포 (= 비용 대시보드) |

**구현에서는 화면 2·3·4를 한 페이지(`run.html`)에 합쳤다.**
실행 하나를 보는 동안 진행·카드·결과가 따로 놀 이유가 없고,
한 화면에서 "무엇을 왜 했고 얼마나 들었는지" 를 다 보게 하려는 의도였다.
실제 페이지는 `start` · `run` · `runs` 셋이다.

**실행 로그 패널**은 실행 화면에 항상 떠 있다. 여기에 남길 것:

```
[09:01:12] 웹검색  검색어="시니어 건강 9월"  → 12건 (1.8초)
[09:01:15] 판단    후보가 넓음 → 사람에게 질문
[09:02:40] 원문조회 3건 대조 → 확인 2 / 미확인 1 (4.2초)
[09:03:05] 이미지  카드2 생성 실패(한도) → 대체 배경 사용
```

각 줄에 **소요 시간**을, 실행 종료 시 **총 토큰·비용**을 기록한다.

### 기술 스택

| 층 | 선택 | 근거 |
|---|---|---|
| 웹 | **FastAPI + Jinja2 + HTMX + SSE** | 파이썬 한 언어, 프로세스 1개, 빌드 도구 없음. **SSE로 실행 로그를 실시간 푸시** |
| 상태 정본 | **SQLite** (WAL 모드) | 중복 차단이 `UNIQUE (run_id, question_id, version)` 한 줄. 대시보드 집계가 쿼리로 나옴 |
| 제출 증거 | **trace.json / outcome.json** | SQLite에서 **내보낸** 파일. 이중 저장이 아니라 "정본 + 내보내기" |
| 루프 실행 | **asyncio 백그라운드 태스크** | 별도 워커(Celery 등)는 배포 단순성을 잃어 쓰지 않는다 |

**Streamlit을 쓰지 않는 이유** — 스크립트 재실행 모델이라 5장이 요구하는
*"새로고침 시 같은 질문 유지"*, *"같은 답변 2번 제출 시 한 번만 실행"*,
*"서버 재시작 후 상태 복원"* 을 구현할 수 없다.

**재시작 복구** — `BackgroundTasks`는 프로세스가 죽으면 유실된다.
상태 정본이 SQLite에 있으므로, 기동 시 `running` 상태인 실행을 찾아 중단 지점부터 이어간다.

---

## 7. 실험 및 평가 설계

### 평가 세트

실제 시나리오(환절기 건강 / 예방접종 안내 / 실내 낙상 예방 등)로 반복 측정한다.
**같은 세팅을 여러 번 재는 것이 핵심이다** — 한 번씩만 재면 세팅 차이인지
그날의 편차인지 구분할 수 없다.

| 지표 | 정의 | 어디서 나오나 |
|---|---|---|
| 완주율 | 마지막 단계까지 끝난 실행 비율 | `outcome.json` 의 `completed` |
| 사람 개입 횟수 | 실행당 질문 수 (적을수록 좋다) | `human_interventions` |
| 루프 반복과 **편차** | 같은 세팅을 반복했을 때의 평균과 폭 | `loop_count` |
| 토큰·비용 | 실행당 입력·출력 토큰과 금액 | `llm_usage` 집계 |
| 도구 실패 | 전체 호출 중 실패 건수와 원인 라벨 | `tool_calls.error_label` |
| 병목 단계 | 어느 단계가 가장 오래 걸렸나 | `bottleneck_step` |

**목표치는 아직 정하지 않았다.** 실행이 더 쌓여야 현실적인 값이 나온다 (9장).
지금은 값을 정하는 것보다 **같은 방법으로 반복해서 재는 것**이 먼저다.

### 🔴 trace 저장 구조 — 설계 단계에 반드시 반영

**이건 나중에 붙일 수 없다.** 로그를 텍스트로만 흘려보내면 평가 표를 만들 수 없다.
매 실행마다 아래 두 파일을 남긴다.

```
runs/<실행ID>/trace.json     ← 도구 호출 순서, 입출력 요약, 판단 근거, 소요시간, 토큰
runs/<실행ID>/outcome.json   ← 완주 여부, 사람 개입 횟수, 재생성 횟수, 실패 라벨
```

**실패 라벨** (원인 분류용): `검색부실` / `사실오류` / `문장어려움` / `이미지실패` / `도구오류` / `중간포기`

이 두 파일이 쌓이면 아래가 모두 **스크립트로 자동 생성**된다.
- `EVAL.md` 의 지표 표
- 화면 5(실행 기록)의 비용 대시보드
- 실패 사례 원인 분포

### 비교할 세팅

제공자와 모델을 바꿔가며 같은 지점까지 돌려 위 지표를 표로 남긴다.
**설정한 모델과 실제로 쓴 모델이 다를 수 있으므로**(무료 한도에 걸리면 폴백한다)
설정 기준으로 묶고 폴백 발생 여부를 함께 적는다. 안 그러면 표가 거짓말을 한다.

```bash
uv run python scripts/compare_settings.py --repeat 3
```

실패한 실행은 지우지 않고 원인 라벨을 붙여 남긴다.

### 개선 사이클

```
실행 → trace·outcome 저장 → 실패 라벨링 → 프롬프트·도구설명·단계구성 개선
   → 같은 평가 세트로 재측정 → before/after 표
```

---

## 8. 배포

| 항목 | 계획 |
|---|---|
| 형태 | 웹앱 (링크만으로 접속 가능) |
| 개발·배포 기기 | **노트북 Windows.** WSL 과 양쪽에 두지 않는다 — 같은 저장소를 두 곳에 두면 어느 쪽이 최신인지 알 수 없게 된다 |
| 방식 | **Cloudflare Tunnel + 로컬 실행.** 같은 기기에서 다른 서비스가 이미 돌고 있어 **전용 터널과 전용 설정 파일**을 따로 만들었다 (D-017) |
| `.venv` | 프로젝트 폴더 안. **폴더를 옮기면 절대경로가 깨져 재생성해야 한다** |
| 비밀정보 | 전부 환경변수. `.env` 커밋 금지 |
| 외부 접속자 보호 | 주소를 아는 사람이 눌러도 **LINE 실발송 불가** (dry-run 고정) |
| 수용 기준 | 아래 다섯 가지. 배포 뒤 실제로 확인했다 |

### 수용 기준과 확인 결과

| 기준 | 결과 |
|---|---|
| 공개 주소로 시작·기록 화면에 접속된다 | 200 응답, 0.4초대 |
| 서버뿐 아니라 데이터베이스까지 정상이다 | `status=ok, db=ok`, 테이블 6개 |
| 주소를 아는 사람이 눌러도 실제 발송이 되지 않는다 | `line_send_enabled=false` |
| 같은 기기의 다른 서비스에 영향이 없다 | 그 서비스도 200 유지 |
| 대표 시나리오에서 카드 5장이 만들어진다 | `EVAL.md` 참고 |

---

## 9. 결정 현황

### 확정된 것 (근거는 `DECISIONS.md`)

| 항목 | 결정 | 근거 |
|---|---|---|
| 도메인 | 시니어 생활정보 안내 | D-002 |
| 출력물 | 카드뉴스 이미지 5장 (1080×1350) | D-003 |
| 글자 처리 | 배경만 생성, 텍스트는 코드로 합성 | D-004 |
| LINE 발송 | 잠금 4개 (dry-run 기본) | D-005 · D-019 |
| n8n | 트리거만. 판단은 코드 | D-006 · D-021 |
| 에이전트 루프 | **직접 구현** (CLI 위임 안 함) | D-007 |
| 확장 항목 | 6개 중 1~5번 구현 완료 | D-008 |
| 캡처 전략 | 구현 중 그때그때 촬영 | D-009 |
| 두뇌 LLM | `gemini-3.5-flash` 기본 + 폴백 사슬. OpenAI 는 비교용 | D-010 · D-014 · D-016 |
| 검색·원문 도구 | Tavily + httpx/trafilatura | D-011 |
| 기술 스택 | FastAPI + Jinja2 + HTMX + SSE / SQLite(WAL) | D-012 |
| 카드 디자인 | 손주 캐릭터 4자세, 색 3+1, 코드 검사 | D-013 · D-015 |
| 배포 | Cloudflare Tunnel 전용 터널 | D-017 |
| 폰트 | Pretendard (SIL OFL, 동봉) | D-018 |
| 종료 조건 | 루프 45 · 재시도 3 · 반려 2 · 질문 3 · 10분 · 이미지 8 · 비용 | D-020 · D-023 · D-024 |

### 남은 것 (⬜)

| # | 항목 | 내용 |
|---|---|---|
| 1 | **OpenAI 계정 하드 리밋 확인** | 기관 지급 크레딧 $5. 계정에 결제수단이 연결돼 있으면 초과분이 기관에 청구된다. 앱은 누적 $3.00 에서 먼저 멈추지만, 대시보드 확인은 별개다 |
| 2 | 평가 지표 목표치 | 완료율·정확도의 목표 수치를 아직 정하지 않았다. 실행이 더 쌓여야 현실적인 값이 나온다 |
| 3 | 카드 버전 롤백 (확장 6) | 재생성 전 카드를 남겨 되돌리기. 확장 6개 중 유일하게 남았다 |
| 4 | 카드별 부분 재생성 | 검토 반려 시 전체를 다시 합성한다. 6장에 적어둔 *"승인된 카드는 유지"* 는 구현되지 않았다 |
| 5 | 테스트 코드 | 없다. 회귀를 막는 장치가 사람 눈뿐이다 |

## 10. MVP 이후 계획

MVP 를 넘어 붙일 것을 여섯 갈래로 정리했다.
**앞의 둘은 사실상 MVP 의 일부다** — 없으면 운영에서 무엇이 잘못됐는지 알 수 없다.

| 순위 | 항목 | 왜 필요한가 | 투입 | 상태 |
|---|---|---|---|---|
| **1** | **trace 저장 → 실패 축적·개선 사이클** | 어느 단계가 병목이고 무엇 때문에 실패했는지 기록이 없으면 고칠 수 없다 | 🟢 낮음 | ✅ 설계 단계에 반영 (7장) |
| **2** | **진행 UI + 비용 대시보드** | 담당자가 "지금 어디까지 왔고 얼마나 썼는지" 를 봐야 한다 | 🟢 낮음 | ✅ MVP 포함 (6장 화면 5) |
| **3** | **자체 MCP 서버** | 운영 데이터(발행 이력·독자 규칙·양식)를 **에이전트가 판단에 쓸 수 있게** 노출 | 🟢 낮음 | ✅ 완료 (4장) |
| **4** | **스케줄 트리거** (n8n) | 주기 업무라 사람이 매번 시작할 이유가 없다 | 🟡 중간 | ✅ 완료 (D-021) |
| **5** | **검토 에이전트** | 만든 쪽이 자기 결과를 검사하면 못 잡는다 | 🔴 높음 | ✅ 완료 (D-020) |
| 6 | 카드 버전 롤백 | 재생성했더니 이전 게 나았던 경우 | 🟡 중간 | ⬜ 남음 |

### 1·2번을 먼저 둔 이유 — 나중에 붙일 수 없다

로그를 텍스트로만 흘려보내면 나중에 표를 만들 수 없고,
비용을 기록하지 않으면 어디서 돈이 새는지 영영 모른다.
그래서 첫 커밋부터 `tool_calls` · `llm_usage` 테이블에 원본을 쌓았다.

### 4번 — 스케줄 트리거가 이 도메인에 맞는 이유

시니어 안내는 **주기 업무**다. 자동 시작과 사람 개입이 결합되면 실무가 이렇게 된다.

```
매주 월요일 06:00  → 자동 시작, 조사까지 마치고 waiting_for_user 로 대기
담당자 출근 09:00  → 질문에 답만 하면 됨
```

**사람이 시작하지 않아도 돌지만, 결정은 사람이 한다.**
n8n 은 트리거만 담당하고 판단은 코드가 한다 (`DECISIONS.md` D-006).

### 5번 — 검토 에이전트를 나중에 붙인 이유

만든 에이전트에게 "잘 만들었니" 를 물으면 대체로 잘 만들었다고 답한다.
자기가 쓴 문장을 자기가 채점하는 셈이라, 날짜를 틀리게 적었어도 그대로 넘어간다.
그래서 **역할이 다른 검토자**를 따로 뒀다. 검토자가 보는 것은 셋이다.

- 카드 문장의 날짜·수치가 근거와 맞는가
- 시니어가 이해 못 할 표현(외래어·전문용어)이 남았는가
- 한 카드에 메시지가 둘인가

값은 높지만 토큰이 늘고 두 에이전트가 주고받는 것을 디버깅해야 해서 뒤로 미뤘다.
**리스크: 검토자가 계속 반려해 루프가 안 끝나는 것** → 반려 상한 2회를 종료 조건에 넣었다 (3장).

### 6번 — 롤백 범위

발송이 마지막이고 그 앞은 전부 승인 전이라 이미 되돌릴 수 있다.
값이 나오는 건 **카드 버전 되돌리기** 하나뿐이라 우선순위가 가장 낮다.

---

## 11. 만든 순서

계획한 대로만 가지 않았다. 아래는 **실제로 진행된 순서**다.

```
PRD 작성 → 스택·도구 결정 → 프로젝트 뼈대 → LLM 어댑터
  → 도구 9종 연결(스키마·설명·실패규칙·권한)
  → 에이전트 루프 + 상태 저장 + 종료 조건
  → 질문/재개 화면 + 실행 로그(SSE)
  → trace/outcome 저장          ← 이걸 먼저 깔아둔 것이 뒤에서 크게 도움이 됐다
  → 카드 디자인·합성 → 검수·내보내기 → 비용 대시보드
  → 배포(Cloudflare Tunnel) → 수용 기준 확인
  → 자체 MCP 서버 → 검토 에이전트 → n8n 스케줄 트리거
  → 쌓인 기록을 다시 읽고 결함 넷을 찾아 고침  ← 완주한 뒤에 나온 것들
```

**마지막 줄이 이 프로젝트에서 가장 값이 컸다.** 돌아가는 것과 제대로 돌아가는 것은 달랐고,
그 차이는 `trace.json` 을 처음부터 쌓아둔 덕에 보였다.
무엇을 찾아 어떻게 고쳤는지는 `EVAL.md` 1장과 `DECISIONS.md` D-022~024 에 있다.
