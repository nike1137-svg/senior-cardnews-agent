# PRD — 시니어 생활정보 카드뉴스 제작 에이전트

- 작성일: 2026-09-09
- 작성자: 조경호
- 상태: **구현 완료** (2026-09-10). 실물과 어긋난 곳은 발견할 때마다 이 문서를 고쳤다
- 관련 문서: [결정 이력](DECISIONS.md) · [캡처 체크리스트](docs/screenshots/README.md)
- 채점 기준(루브릭)과 강의 자료는 LMS 원문이라 이 저장소에 두지 않는다
  (사본: `C:\Users\nike1\projects\notes\mainquest4-lms\`)

---

## 1. 문제 정의

### 어떤 반복 업무인가

경로당·복지관·시니어 대상 채널에 **주기적으로 생활정보 안내물을 만들어 올리는 일**.
지금은 사람이 매번 다음을 반복한다.

1. 이번 주에 알릴 만한 소식을 여기저기서 찾는다 (날씨, 건강, 복지 공고)
2. 시니어가 이해할 수 있는 말로 다시 쓴다
3. 큰 글씨로 이미지를 만든다
4. 발송한다

찾는 데 오래 걸리고, **매번 품질이 들쭉날쭉**하며, 출처를 남기지 않아 나중에 확인이 안 된다.

### 입력 → 결과물

| 구분 | 내용 |
|---|---|
| **입력** | 주제 한 줄 (예: "이번 주 건강·생활 정보") + 대상 지역 |
| **결과물** | 카드뉴스 이미지 **5장 (PNG, 1080×1350)** + 전체 ZIP + **출처 기록 파일** |
| **선택 출력** | 승인 시 LINE 발송 (기본값은 발송하지 않음) |

### 이 에이전트가 끝내주는 것

"주제 한 줄" → "**바로 배포할 수 있는 카드뉴스 묶음**" 까지. 사람은 **고르고 승인만** 한다.

---

## 2. 타겟 유저

| 구분 | 대상 | 특징 |
|---|---|---|
| **직접 사용자** | 시니어 대상 채널 운영자 (경로당 담당자, 복지관 직원, 챗봇 운영자) | 디자인 도구를 다루지 못함. 짧은 시간에 만들어야 함 |
| **최종 수신자** | 60~80대 시니어 | 작은 글씨를 못 읽음. 외래어·전문용어에 막힘. 링크를 잘 누르지 않음 |

최종 수신자 특성이 **카드 디자인 제약**으로 직결된다.

- 제목 글자 크기 하한선을 둔다 ⬜ (기준값 확정 필요)
- 한 카드에 메시지 **하나만**
- 외래어는 우리말로 풀어 쓴다
- 숫자·날짜는 반드시 명시 ("다음 주" ❌ → "9월 15일 월요일" ⭕)

---

## 3. 워크플로 설계

### 단계별 — 에이전트가 판단할 일 / 사람이 처리할 일

| # | 단계 | 에이전트가 판단하는 것 | 사람이 하는 것 |
|---|---|---|---|
| 1 | **조사** | 검색어를 정하고, 최근 7일 자료를 모아 **후보 7~12개**로 추림. 자료가 부족하면 기간을 넓힐지 **스스로 판단해 질문** | — |
| 2 | **후보 선택** | — | **소식 1~3개 선택** ⭐ |
| 3 | **심층 검증** | 원문을 열어 날짜·수치 대조. **확인된 사실 / 발표자 주장 / 미확인** 으로 분류 | — |
| 4 | **스토리보드** | 카드 5장의 역할·문구·삽화 계획 수립 | **검토·승인** ⭐ |
| 5 | **이미지 생성** | 카드별 배경 이미지 생성. 실패 시 대체 경로 판단 | — |
| 6 | **카드 합성** | 배경 위에 텍스트 레이어 합성, 글자 가독성 검사 | — |
| 7 | **검수** | 문제 카드 식별 | **부분 수정 지시 / 승인** ⭐ |
| 8 | **내보내기** | PNG·ZIP·출처기록 생성 | 다운로드 |
| 9 | **발송** | — | **발송 승인** ⭐ (기본 미발송) |

⭐ = 사람 개입 지점 (4곳)

### 루프 구조

에이전트는 각 단계 종료 시 **결과를 관찰하고 다음 행동을 결정**한다.

```
계획 → 도구 호출 → 결과 관찰 → 판단
                                 ├ 충분하다      → 다음 단계
                                 ├ 부족하다      → 같은 단계 재시도 (조건 바꿔서)
                                 ├ 사람이 정해야 → status=waiting_for_user
                                 └ 불가능하다    → 대체 경로 또는 중단
```

### 종료 조건 (무한 루프 방지)

| 항목 | 상한 | 넘으면 |
|---|---|---|
| 단계별 재시도 | **3회** | 해당 단계 실패 처리 후 사람에게 보고 |
| 전체 루프 반복 | **30회** | 중단, 진행분 보존. 20회로 뒀다가 올렸다 — 검토 에이전트가 반려하면 재작업에 5~6회가 더 든다 (D-020) |
| 전체 실행 시간 | **10분** | 중단, 진행분 보존 |
| 이미지 생성 호출 | **1회 실행당 8회** | 대체 경로(단색 배경)로 전환 |
| LLM 호출 (Gemini 경로) | **1회 실행당 40회 · 일일 300회** | 중단 (무료 티어라 과금은 없음) |
| 🔴 LLM 비용 (OpenAI 경로) | **1회 실행당 $0.30 · 누적 $3.00** | 즉시 중단. 기관 지급 크레딧이므로 초과 과금을 앱이 먼저 막는다 |
| 검토 에이전트 반려 | **2회** | 검토자가 계속 반려해 루프가 안 끝나는 것을 막는다. 상한 도달 시 사람에게 넘김 (확장 5번 적용 시) |

---

## 4. 두뇌(LLM)와 도구 계획

### 두뇌 — LLM 어댑터 (교체 가능)

루브릭은 **모델을 지정하지 않는다.** 오히려 실험·평가 항목이
*"프롬프트, 도구 설명, **모델**, 단계 구성 등 세팅을 바꿔 가면서 성능 변화를 표로"* 를 요구한다.
따라서 제공자를 어댑터로 감싸 **설정 한 줄로 교체 가능**하게 만든다.

| 역할 | 모델 | 근거 |
|---|---|---|
| **기본** | **`gemini-3.5-flash`** | 무료 티어. 계획 시점에 적었던 2.5 Flash 는 실제로 쓸 수 없었다 (D-014) |
| **폴백 사슬** | `gemini-3.5-flash-lite` → `gemini-3-flash-preview` → … | 무료 티어는 **모델당 하루 20회**라 한 모델로는 못 버틴다. 429·503 에 다음 모델로 갈아탄다 (D-016) |
| **비교·시연** | OpenAI GPT-5 mini | 어댑터는 구현했으나 **키를 넣지 않아 미사용.** 모델 비교는 Gemini 계열 3종으로 했다 (`EVAL.md` 2장) |

**필수 조건: 함수 호출(function calling)을 지원하는 API여야 한다.**
D-007에서 루프를 직접 구현하기로 했으므로, 내 앱이 도구를 정의하고 모델이 그중에서 고른다.
`claude -p` · `opencode run` 같은 **CLI 위임 방식은 쓰지 않는다** — CLI 자체가 에이전트여서
**내가 정의한 도구 스키마가 남지 않고**, 루브릭 2번의 증거가 약해진다.

**왜 개발을 무료 모델로 하나** — $5는 개발 예산이 아니라 시연 예산이다.
평가 세트만 해도 시나리오 5개 × 세팅 3개 = 15회이고, 개발 중 실패한 실행도 토큰을 먹는다.
GPT-5(정품)로 개발하면 약 7회에 소진된다.

**🔒 9번 규칙** — Gemini 무료 티어는 입력이 학습에 쓰일 수 있다. 프롬프트에 **절대 넣지 않는다**:
LINE 채널 토큰, `.env` 내용, 온담이·이음이 자격증명. 이 프로젝트가 다루는 것은 **공개 뉴스·날씨**뿐이다.
도구 호출 결과를 프롬프트에 담을 때 키가 섞이지 않는지 확인하는 필터를 둔다.

### 연결할 도구

| # | 도구 | 용도 | 입력 | 출력 | 권한 | 위험도 |
|---|---|---|---|---|---|---|
| 1 | **웹 검색** (Tavily) | 최근 소식 조사 | 검색어, 기간 | 제목·링크·게시일 목록 | 읽기 전용, **무료 1,000크레딧/월·카드 불필요** | 🟢 |
| 2 | **원문 조회** (httpx + trafilatura) | 링크 열어 날짜·수치 대조 | URL | 본문 텍스트 | 읽기 전용, **로컬·완전 무료** | 🟢 |
| 3 | **날씨 조회** (Open-Meteo) | 지역 날씨·주의사항 | 위경도, 날짜 | 기온·강수·특보 | 읽기 전용, **키 불필요** | 🟢 |
| 4 | **이미지 생성** (Antigravity CLI) | **캐릭터 자산 제작(4자세).** 카드 배경은 코드로 그려 실행마다 부르지 않는다 (D-014) | 프롬프트 | PNG 1024×1024 | 파일 쓰기 (지정 폴더만) | 🟡 |
| 5 | **카드 합성** (로컬, Pillow) | 배경 + 텍스트 레이어 | 배경 PNG, 문구 | PNG 1080×1350 | 파일 쓰기 (지정 폴더만) | 🟢 |
| 6 | **LINE 발송** | 최종 배포 | 이미지, 대상 | 발송 결과 | **기본 dry-run**, 실발송은 승인 필요 | 🔴 |

> 루브릭 요구는 "2개 이상". 위는 6개이며, 이 중 **1·3·4·6은 외부 시스템** 호출이다.

### 자체 MCP 서버로 노출할 도구 (확장 3번)

*"내 도메인의 시스템을 에이전트의 도구로 노출"* — 이 프로젝트에서 그 시스템은 **안내 운영 데이터**다.
별도 MCP 서버(`senior-cardnews-mcp`)를 만들어 아래를 도구로 제공한다.

| # | 도구 | 하는 일 | 권한 |
|---|---|---|---|
| 7 | `list_past_publications` | 과거 발행 이력 조회 | 읽기 |
| 8 | `get_audience_profile` | 시니어 독자 특성 (금지 표현, 글자 크기 하한, 관심 주제) | 읽기 |
| 9 | `get_card_template` | 카드 양식 (색·레이아웃·문구 규칙) | 읽기 |
| 10 | `record_publication` | 발행 결과 기록 | 쓰기 (지정 저장소만) |

**단순 가점이 아니라 루프를 강화한다.** `list_past_publications` 덕분에 에이전트가 이렇게 판단할 수 있다.

```
후보 12개 수집 → 과거 이력 조회 → "독감 예방접종은 3주 전에 이미 다룸"
              → 판단: 후순위로 내리고 사람에게 알림
```

도구를 하나 더 붙인 것이 아니라, **에이전트가 판단할 근거를 하나 더 준 것**이다.
`record_publication` 이 다음 실행의 입력이 되므로 **사이클**이 형성된다.

> 배포 시 프로세스가 늘어나는 것을 피하려면 웹앱과 **같은 호스트에서 stdio로** 띄운다.

### 권한 최소화 원칙

- **읽기 도구와 쓰기 도구를 분리**한다. 조사 단계는 쓰기 권한을 갖지 않는다
- 파일 쓰기는 `output/<실행ID>/` **한 폴더 안으로만** 제한한다
- **Antigravity CLI에 `--dangerously-skip-permissions` 를 쓰지 않는다.** 필요한 도구만 개별 허용
- **LINE 발송은 기본값이 `dry-run`** 이다. 실제 발송은 ①사람 승인 ②환경변수 스위치, 두 조건이 모두 충족될 때만
- API 키는 전부 환경변수. 저장소에 커밋하지 않는다 (`.env` 는 `.gitignore`)

### 도구 실패 처리 규칙

| 도구 | 실패 상황 | 처리 |
|---|---|---|
| 웹 검색 | 결과 0건 | 검색어를 바꿔 **2회 재시도** → 그래도 없으면 "기간을 넓힐까요?" **사람에게 질문** |
| 원문 조회 | 페이지 열기 실패 | 해당 항목을 **"미확인"으로 분류**하고 계속 진행 (조용히 버리지 않음) |
| 날씨 조회 | 응답 없음 | 날씨 카드를 **건너뛰고** 나머지로 진행, 로그에 기록 |
| **이미지 생성** | **한도 초과 / 권한 거부** | ①1회 재시도 → ②**단색·패턴 배경으로 대체** → ③카드는 반드시 완성. 대체했음을 화면에 표시 |
| 카드 합성 | 글자가 배경에 묻힘 | 텍스트 뒤에 반투명 판을 깔고 재합성 |
| LINE 발송 | 발송 실패 | **재시도하지 않는다** (중복 발송 위험). 실패를 표시하고 사람이 다시 누르게 함 |

> ⚠️ **"종료 코드 0" 을 성공으로 보지 않는다.** 이미지 도구는 **파일이 실제로 생성되고 열리는지**를 확인한 뒤에만 성공 처리한다.

---

## 5. 사람 개입 지점

| # | 지점 | 왜 사람이 필요한가 | 되돌릴 수 있나 |
|---|---|---|---|
| 1 | 소식 선택 (2단계) | 무엇이 우리 시니어에게 의미 있는지는 사람이 안다 | ⭕ |
| 2 | 스토리보드 승인 (4단계) | 이미지 생성 **전에** 잡아야 한도 낭비가 없다 | ⭕ |
| 3 | 카드 검수 (7단계) | 오탈자·가독성은 사람 눈이 필요 | ⭕ |
| 4 | **발송 승인 (9단계)** | **발송은 취소가 불가능하다** | ❌ |

### 질문·재개 규칙

에이전트가 질문을 만들면 상태를 `waiting_for_user` 로 저장하고 화면에 질문과 선택지를 띄운다.
답변이 오면 `running` 으로 바꾸고 **저장된 세션 ID로 이어간다.**

```json
{
  "status": "need_input",
  "question_id": "audience-001",
  "version": 1,
  "question": "어떤 내용을 중심으로 담을까요?",
  "options": ["건강 정보", "복지 혜택", "날씨·생활 주의사항"],
  "multi_select": false
}
```

반드시 지킬 것:

| 상황 | 기대 동작 |
|---|---|
| 질문 상태에서 새로고침 | 같은 질문 유지, 답변 전엔 진행 안 함 |
| 같은 답변 2번 제출 | **한 번만** 실행 (`question_id` + `version` 으로 중복 차단) |
| 지난 질문에 뒤늦게 답변 | "지난 질문입니다" 안내, 새 작업 시작 안 함 |
| 실패 후 재시도 | 실패 원인 표시, **성공한 카드는 보존** |
| 서버 재시작 후 접속 | 저장된 상태 복원, 중단 지점부터 재시도 가능 |

---

## 6. 핵심 기능(MVP)과 화면 구성

### MVP 기능 목록

- [x] 주제 입력 → 조사 → 후보 목록 제시
- [x] 후보 선택 (1~3개)
- [x] 심층 검증 및 사실/주장/미확인 분류 표시
- [x] 스토리보드 생성 및 승인
- [x] 카드 이미지 생성 (배경) + 텍스트 레이어 합성
- [~] **카드별 부분 재생성은 만들지 않았다.** 검토 에이전트가 반려하면 전체를 다시 합성한다.
  텍스트가 별도 레이어라 배경 이미지를 다시 만들지는 않아 비용은 들지 않지만,
  *"승인된 카드는 유지"* 는 구현되지 않았다. 재합성 횟수는 `trace.json` 의 `regenerations` 로 센다
- [x] PNG / ZIP / 출처기록 내보내기
- [x] **실행 로그 화면 표시** (도구명·이유·결과·소요시간)
- [x] **상태 저장 및 재개** (서버 재시작 견딤)
- [x] LINE 발송 (dry-run 기본 · 실채널 확인 D-019)

### 화면 구성

| 화면 | 담는 것 |
|---|---|
| **1. 시작** | 주제 입력, 지역 선택, 시작 버튼 |
| **2. 진행** | 현재 단계 / **질문 카드**(선택지+답변) / **실행 로그 패널**(실시간) |
| **3. 검수** | 카드 미리보기(모바일 크기), 카드별 재생성, 전체 승인 |
| **4. 결과** | PNG·ZIP 다운로드, 출처 기록, 발송 버튼(경고 문구 포함) |
| **5. 실행 기록** | 지난 실행 목록, 단계별 소요시간·토큰·비용, 실패 라벨 분포 (= 비용 대시보드) |

**실행 로그 패널**은 화면 2·3에 항상 떠 있다. 여기에 남길 것:

```
[09:01:12] 웹검색  검색어="시니어 건강 9월"  → 12건 (1.8초)
[09:01:15] 판단    후보가 넓음 → 사람에게 질문
[09:02:40] 원문조회 3건 대조 → 확인 2 / 미확인 1 (4.2초)
[09:03:05] 이미지  카드2 생성 실패(한도) → 대체 배경 사용
```

각 줄에 **소요 시간**을, 실행 종료 시 **총 토큰·비용**을 기록한다.

### 기술 스택

| 층 | 선택 | 근거 |
|---|---|---|
| 웹 | **FastAPI + Jinja2 + HTMX + SSE** | 파이썬 한 언어, 프로세스 1개, 빌드 도구 없음. **SSE로 실행 로그를 실시간 푸시** |
| 상태 정본 | **SQLite** (WAL 모드) | 중복 차단이 `UNIQUE (run_id, question_id, version)` 한 줄. 대시보드 집계가 쿼리로 나옴 |
| 제출 증거 | **trace.json / outcome.json** | SQLite에서 **내보낸** 파일. 이중 저장이 아니라 "정본 + 내보내기" |
| 루프 실행 | **asyncio 백그라운드 태스크** | 별도 워커(Celery 등)는 배포 단순성을 잃어 쓰지 않는다 |

**Streamlit을 쓰지 않는 이유** — 스크립트 재실행 모델이라 5장이 요구하는
*"새로고침 시 같은 질문 유지"*, *"같은 답변 2번 제출 시 한 번만 실행"*,
*"서버 재시작 후 상태 복원"* 을 구현할 수 없다.

**재시작 복구** — `BackgroundTasks`는 프로세스가 죽으면 유실된다.
상태 정본이 SQLite에 있으므로, 기동 시 `running` 상태인 실행을 찾아 중단 지점부터 이어간다.

---

## 7. 실험 및 평가 설계

### 평가 세트

실제 시나리오 ⬜**N개** (예: 건강 / 복지공고 / 날씨주의 / 지역행사 / 사기예방) 로 반복 측정.

| 지표 | 정의 | 목표 |
|---|---|---|
| 작업 완료율 | 사람 개입 최소 횟수로 끝까지 완주한 비율 | ⬜ |
| 사실 정확도 | 카드에 실린 날짜·수치가 원문과 일치하는 비율 | ⬜ |
| 소요 시간 | 시작~내보내기 | ⬜ |
| 비용 | 1회 실행당 토큰 + 이미지 호출 수 | ⬜ |
| 재생성 횟수 | 사람이 카드를 다시 만들라고 한 횟수 | ⬜ |

### 🔴 trace 저장 구조 — 설계 단계에 반드시 반영

**이건 나중에 붙일 수 없다.** 로그를 텍스트로만 흘려보내면 평가 표를 만들 수 없다.
매 실행마다 아래 두 파일을 남긴다.

```
runs/<실행ID>/trace.json     ← 도구 호출 순서, 입출력 요약, 판단 근거, 소요시간, 토큰
runs/<실행ID>/outcome.json   ← 완주 여부, 사람 개입 횟수, 재생성 횟수, 실패 라벨
```

**실패 라벨** (원인 분류용): `검색부실` / `사실오류` / `문장어려움` / `이미지실패` / `도구오류` / `중간포기`

이 두 파일이 쌓이면 아래가 모두 **스크립트로 자동 생성**된다.
- `EVAL.md` 의 지표 표
- 화면 5(실행 기록)의 비용 대시보드
- 실패 사례 원인 분포

### 비교할 세팅

프롬프트 / 도구 설명(description) / 모델 / 단계 구성을 바꿔가며 위 지표를 표로 남긴다.
실패한 실행은 원인 단계별로 분류해 정리한다.

### 개선 사이클

```
실행 → trace·outcome 저장 → 실패 라벨링 → 프롬프트·도구설명·단계구성 개선
   → 같은 평가 세트로 재측정 → before/after 표
```

---

## 8. 배포

| 항목 | 계획 |
|---|---|
| 형태 | 웹앱 (링크만으로 접속 가능) |
| 개발·배포 기기 | **노트북 Windows** (`C:\Users\nike1\projects\study\mainquest4-cardnews`). WSL과 양쪽에 두지 않는다 |
| 방식 | **Cloudflare Tunnel + 로컬 실행.** 제출 항목에서 배포 URL이 빠졌으므로 **상시 켜둘 필요 없음** — 캡처를 남길 동안만 띄운다 |
| `.venv` | 프로젝트 폴더 안. **폴더를 옮기면 절대경로가 깨져 재생성해야 한다** |
| 비밀정보 | 전부 환경변수. `.env` 커밋 금지 |
| 채점자 보호 | 채점자 접속 시 **LINE 실발송 불가** (dry-run 고정) |
| 수용 기준 | ⬜ 대표 시나리오 N개 중 M개가 사람 개입 4회 이하로 완주 |

---

## 9. 결정 현황

### 확정된 것 (근거는 `DECISIONS.md`)

| 항목 | 결정 | 근거 |
|---|---|---|
| 도메인 | 시니어 생활정보 안내 | D-002 |
| 출력물 | 카드뉴스 이미지 5장 (1080×1350) | D-003 |
| 글자 처리 | 배경만 생성, 텍스트는 코드로 합성 | D-004 |
| LINE 발송 | 3중 잠금 (dry-run 기본) | D-005 |
| n8n | MVP 제외, 확장 4번으로 트리거만 | D-006 |
| 에이전트 루프 | **직접 구현** (CLI 위임 안 함) | D-007 |
| 확장 항목 | 6개 중 1~4번 구현 약속 | D-008 |
| 캡처 전략 | 구현 중 그때그때 촬영 | D-009 |
| **두뇌 LLM** | **Gemini 2.5 Flash 기본 + OpenAI GPT-5 mini 비교·시연** | **D-010** |
| **검색·원문 도구** | **Tavily + httpx/trafilatura** | **D-011** |
| **기술 스택·기기** | **FastAPI+HTMX+SSE / SQLite / 노트북 Windows** | **D-012** |

### 남은 미결정 (⬜)

| # | 항목 | 왜 필요한가 | 정할 시점 |
|---|---|---|---|
| 1 | **카드 양식** (색·폰트·레이아웃) | 참조본 대기 중. **없으면 기본안으로 시작 가능** — `get_card_template` MCP 도구가 있어 나중에 교체해도 코드 수정 불필요 | 카드 합성 전 |
| 2 | **폰트 확정** | Pretendard / 나눔스퀘어 / Noto Sans KR 중 택1. 셋 다 상업적 이용 가능한 오픈 라이선스. **시니어 대상이므로 Bold 이상.** 라이선스 파일을 저장소에 동봉 | 카드 합성 전 |
| 3 | **LINE 실채널 사용 여부** | 별도 테스트 채널을 팔지, dry-run만으로 끝낼지. **구현은 동일**하므로 미뤄도 무방 | 시연 계획 확정 시 |
| 4 | **Antigravity CLI 설치 + 한도 실측** | **이 기기에 미설치 확인됨.** 하루 몇 장까지 되는지 직접 재야 한다 | 5단계 착수 직전 |
| 5 | 🔴 **OpenAI 키의 하드 리밋 확인** | 기관 지급 크레딧 $5. **계정에 결제수단이 연결돼 있으면 초과분이 기관에 청구된다.** 대시보드 Usage limits 확인 | OpenAI 경로 첫 호출 전 |
| 6 | 평가 지표 목표치 | 1회 실행해본 뒤 현실적인 값으로 | 구현 후 |
| 7 | 퍼실 확인 5건 | `DECISIONS.md` 맨 아래 | 답 받는 즉시 |

---

## 10. 확장 계획 (루브릭 5번 가점)

루브릭의 *"조금 더 발전시키고 싶다면"* 항목을 전부 검토했다.
**원문은 불릿 5개**이고, 마지막 불릿이 *진행 UI · 되돌리기 · 비용 대시보드* 셋을 한 줄에 묶고 있어
**낱개로 세면 7개**다. 아래 표는 그것을 6줄로 재편성한 것이다.

**핵심: 1·2번은 루브릭 필수 항목의 연장선이라 추가 작업이 거의 없다.**
다만 🔴 **평가 문항 5번이 이름을 지목하는 것은 "MCP 서버 제작·멀티 에이전트·자동 트리거"** 다.
1·2번은 평가 4번의 필수 요소이지 5번 가점이 아니므로, **5번을 채우려면 3번(MCP 서버)이 최소 요건**이다.

| 순위 | 항목 | 루브릭 필수와의 관계 | 투입 | 언제 |
|---|---|---|---|---|
| **1** | **trace 저장 → 실패 축적·개선 사이클** | 4번이 이미 요구 | 🟢 낮음 | **설계 단계 (7장)** |
| **2** | **진행 UI + 비용 대시보드** | 4번이 이미 요구 | 🟢 낮음 | **MVP 포함 (6장 화면 5)** |
| **3** | **자체 MCP 서버** (운영 데이터 노출) | 2번 도구 예시에 MCP | 🟢 낮음 | MVP 직후 (4장) |
| **4** | **스케줄 트리거** (n8n) | 필수 아님 — 순수 가점 | 🟡 중간 | **워크플로 작성 완료** `deploy/n8n/` · 임포트 대기 (D-021) |
| 5 | 검토 에이전트 (멀티 에이전트) | 3번에 "필요하면" — 선택 | 🔴 높음 | 여유 시 |
| 6 | 카드 버전 롤백 | 필수 아님 | 🟡 중간 | 여유 시 |

**1~4번은 구현 약속. 5~6번은 여유가 생기면.**

### 4번 — 스케줄 트리거가 이 도메인에 특별히 맞는 이유

시니어 안내는 주기적 업무다. 자동 트리거 + HITL이 결합되면 실무 시나리오가 이렇게 된다.

```
매주 월요일 06:00  → 자동 시작, 조사까지 마치고 waiting_for_user 로 대기
담당자 출근 09:00  → 질문에 답만 하면 됨
```

**"사람이 시작하지 않아도 도는데, 결정은 사람이 하는" 구조** — 루브릭이 원하는 그림 그대로다.
n8n은 트리거만 담당하고 판단은 코드가 한다 (`DECISIONS.md` D-006 참조).

### 5번 — 검토 에이전트를 왜 나중으로 미루나

루브릭 4번의 *"에이전트가 '작업을 끝냈다'고 말하는 것과 실제 결과물이 만들어진 것은 다르다"* 를
구조적으로 해결하는 방법이라 가치는 높다. 검토자가 할 일도 명확하다.

- 카드 문장의 날짜·수치를 원문과 대조
- 시니어가 이해 못 할 표현(외래어·전문용어)이 남았는지 검사
- 글자가 배경에 묻혔는지 검사

다만 토큰이 늘고, 두 에이전트가 주고받는 걸 디버깅해야 한다.
**리스크: 검토자가 계속 반려해 루프가 안 끝나는 사고** → 종료 조건에 반려 상한 2회를 이미 넣어뒀다 (3장).

### 6번 — 롤백 범위

발송이 마지막 단계이고 그 앞은 전부 승인 전이라 이미 되돌릴 수 있다.
값이 나오는 건 **"카드 버전 되돌리기"** 하나 — 재생성했더니 이전 게 나았던 경우.
카드별로 이전 버전을 남겨두면 된다.

---

## 11. 진행 순서

```
PRD 확정 → 미결정 1·2 결정 → 프로젝트 뼈대
  → 도구 6종 연결(스키마·실패규칙) → 에이전트 루프 + 상태 저장
  → 질문/재개 화면 + 실행 로그 패널 → trace/outcome 저장 ⭐확장1
  → 카드 합성 → 검수·내보내기 → 비용 대시보드 화면 ⭐확장2
  → 실험·평가 표 → 배포 → 수용 기준 점검
  ───── 여기까지 MVP ─────
  → MCP 서버 ⭐확장3 → n8n 스케줄 트리거 ⭐확장4
  → (여유 시) 검토 에이전트 ⭐확장5 · 카드 롤백 ⭐확장6
```
