# DECISIONS — 결정 이력

> 무엇을, 왜 골랐는지 남긴다. 나중에 "왜 이렇게 했지?" 를 다시 묻지 않기 위함이고,
> 제출물이기도 하다 — **내 선택으로 무엇이 달라졌는지**를 보여주는 문서다.

상태 표기: ✅ 확정 · 🟡 잠정(확인 필요) · ⬜ 미결정

---

## D-001 ✅ 과제 요구사항을 정본으로, 예시 자료는 참고로

**날짜** 2026-09-09

**결정** 지켜야 할 기준은 **과제 요구사항**이고, 함께 주어진 예시 자료는 참고로만 쓴다.

> 둘 다 과제 자료라 **이 저장소에 두지 않는다.** 요구사항은 `PRD.md` 에 자체 언어로 옮겼다.

**이유** 두 쪽의 범위가 달랐다.

| 항목 | 예시 자료 | 요구사항 |
|---|---|---|
| 주제 | 카드뉴스로 고정 | 자기 도메인 자유 |
| 배포 | 로컬 실행 | **접속 가능한 URL 필수** |
| 실행 엔진 | 3종 택1 | 자유 |
| 평가 | 체크리스트 | **정량 지표 + 세팅 비교 표** |

**영향** 배포·정량평가·실행로그·종료조건이 새로 필수가 됐다. 예시 자료에는 없던 항목이다.
예시를 그대로 따라갔으면 넷 다 빠졌을 것이다.

---

## D-002 ✅ 도메인 — 시니어 생활정보 안내

**날짜** 2026-09-09

**결정** 시니어 대상 생활정보 안내물 제작을 자동화한다.

**검토한 대안**

| 후보 | 탈락/선택 이유 |
|---|---|
| 블로그 발행 자동화 | 평가 세트 만들기는 쉬우나 **에이전트 루프가 직선 파이프라인**이 되기 쉽다 |
| 네트워크 장애 대응 | 경력이 가장 살지만, **실제 장애 로그가 사내 자산**이라 "링크만으로 접속해 실행" 배포 요건과 충돌 |
| **시니어 정보 안내** ⭕ | 타겟이 실존하고, **발송이 되돌릴 수 없는 작업**이라 HITL 승인 지점이 억지가 아님 |

**영향** PRD 2장(타겟 유저)이 구체화됐고, 가독성 제약(글자 크기·쉬운 말)이 설계 조건으로 들어왔다.

---

## D-003 ✅ 출력 형태 — 카드뉴스 이미지 (A안)

**날짜** 2026-09-09

**결정** 최종 산출물은 카드뉴스 이미지 5장(PNG 1080×1350) + ZIP + 출처 기록.

**검토한 대안**

| 안 | 내용 | 판단 |
|---|---|---|
| **A** ⭕ | 카드뉴스 이미지 | 선택. 강의 자료를 재활용할 수 있고 결과물이 눈에 보임 |
| B | 큰 글씨 텍스트 메시지 | 이미지 한도 리스크가 없어 완주 확률은 높으나, 시연 임팩트가 약함 |
| C | A4 안내문 PDF | 중간. 인쇄 배포에 유리하나 채점 시연에서 덜 직관적 |

**리스크와 대응** A안은 Antigravity 무료 한도에 걸릴 수 있다.
→ **생성 실패 시 단색·패턴 배경으로 대체**해 카드를 반드시 완성시킨다.
덕분에 한도에 걸려도 카드가 반드시 완성된다. 시연 중 화면이 비지 않는다.

---

## D-004 ✅ 글자는 이미지에 그리지 않고 별도 레이어로 합성

**날짜** 2026-09-09

**결정** 이미지 생성 도구는 **배경만**(1024×1024) 만들고, 텍스트는 코드로 얹어 1080×1350으로 완성한다.

**이유**
1. 이미지 모델이 그린 글자는 오탈자가 나고 **수정이 불가능**하다
2. 분리해야 **문구만 고칠 때 이미지를 다시 만들지 않는다** → 이미지 한도 절약 + 부분 수정 가능
3. 시니어 대상이라 **글자 크기를 코드로 강제**해야 한다

---

## D-005 ✅ LINE 발송 3중 잠금

**날짜** 2026-09-09

**결정** 발송 도구의 기본값은 `dry-run`. 실제 발송은 ①사람 승인 ②환경변수 스위치가 모두 켜져야 한다.
발송 실패 시 **자동 재시도하지 않는다.**

**이유** 배포 주소를 공개하는데, **주소를 아는 사람이 버튼을 눌러 실제 시니어에게 메시지가 나가면 안 된다.**
자동 재시도는 중복 발송 위험이 있어 일부러 뺐다.

**영향** 되돌릴 수 없는 도구에는 권한을 최소화하고 실행 전에 사람이 확인하는 구조가 됐다.

---

## D-006 ✅ n8n은 MVP에 넣지 않고, 나중에 트리거만 얹는다

**날짜** 2026-09-09 · **확정** (확장 계획 4번으로 채택 — D-008 참조)

**결정(안)** 1단계는 n8n 없이 완주하고, 여유가 있으면 2단계에서 스케줄·웹훅 트리거만 n8n으로 붙인다.

**배경** n8n 은 이미 다른 프로젝트에서 써 본 도구다.
다만 **쓰는 방식에 따라 판단이 갈린다.**

| n8n 사용법 | 에이전트인가 |
|---|---|
| 노드를 순서대로 늘어놓은 일반 워크플로 | ❌ 자동화 |
| AI Agent 노드 사용 | ⭕ |
| n8n은 트리거·발송만, 판단은 코드 | ⭕ |

**두뇌를 n8n에 넣지 않는 이유**
실행 로그와 질문/재개 화면이 필요한데, **n8n 편집 화면은 외부에 열 수 없다**
(자격증명과 다른 워크플로가 노출된다). 앞단 웹 화면은 어차피 따로 만들어야 하므로,
두뇌를 n8n에 가둘 이점이 약하다.

**단계를 나누는 이유** 처음부터 끼우면 디버깅 대상이 코드 + n8n 연동 두 개로 늘어난다.
n8n은 **있으면 좋은 것이지 MVP 의 필수 요소가 아니다.**

---

## D-007 ✅ 에이전트 루프는 직접 구현

**날짜** 2026-09-09 · **확정**

**결정(안)** 프레임워크에 얹지 않고 파이썬으로 직접 구현한다.

**이유** 종료 조건(재시도 3회/루프 20회/10분/이미지 8회 — *당시 값. 이후 두 번 조정해
루프는 45 가 됐다, D-020 · D-024*)과 상태 저장·재개를 **직접 통제**해야
무엇이 왜 멈췄는지를 직접 설명할 수 있다. 프레임워크에 얹으면 내부 동작이 블랙박스가 된다.

---

## D-008 ✅ 확장 항목 여섯 개의 우선순위

**날짜** 2026-09-09

**결정** 더 붙일 수 있는 것을 전부 훑고 우선순위를 매겼다.
**1번부터 4번까지는 구현하기로 하고, 5·6번은 여유가 생기면** 하는 것으로 뒀다.

| 순위 | 항목 | 투입 | 판정 |
|---|---|---|---|
| 1 | trace 저장 → 실패 축적·개선 사이클 | 🟢 | **설계 단계에 반영** |
| 2 | 진행 UI + 비용 대시보드 | 🟢 | **MVP 포함** |
| 3 | 자체 MCP 서버 (운영 데이터 노출) | 🟢 | MVP 직후 |
| 4 | n8n 스케줄 트리거 | 🟡 | MVP 완주 후 |
| 5 | 검토 에이전트 (멀티 에이전트) | 🔴 | 여유 시 |
| 6 | 카드 버전 롤백 | 🟡 | 여유 시 |

**판단 근거** 6개의 성격이 갈린다.

- **1·2번은 없으면 운영이 안 되는 것**이다. 실행 로그와 비용 기록이 없으면
  무엇이 잘못됐는지 알 수 없다. 추가 기능이 아니라 **기본 설비**에 가깝다
- **3번(MCP)은 값이 크다.** `list_past_publications` 로 과거 발행 이력을 조회하면
  에이전트가 "이 주제는 3주 전에 이미 다뤘다"고 판단할 수 있다.
  도구를 늘린 게 아니라 **판단 근거를 늘린 것**이다.
  MCP 서버는 앞선 실습에서 만들어본 적이 있어 투입도 낮다
- **4번은 이 도메인에 특별히 맞는다.** 시니어 안내는 주기적 업무여서
  "월요일 06시 자동 시작 → 조사 후 대기 → 담당자가 출근해 답만 함" 시나리오가 자연스럽다
- **5번은 값은 높지만 비싸다.** 토큰이 늘고 두 에이전트 간 디버깅이 필요하며,
  검토자가 계속 반려해 루프가 안 끝나는 사고 위험이 있다 (→ 반려 상한 2회를 종료 조건에 추가)
- **6번은 되돌릴 대상이 적다.** 발송이 마지막이고 그 앞은 전부 승인 전이라 이미 되돌릴 수 있다.
  값이 나오는 건 "카드 버전 되돌리기" 하나

**⚠️ 1번은 나중에 붙일 수 없다.** 로그를 텍스트로만 흘려보내면 평가 표를 만들 수 없다.
그래서 `runs/<실행ID>/trace.json` · `outcome.json` 저장 구조를 PRD 7장에 확정해뒀다.

### 이후 결과 (2026-09-10)

| 순위 | 항목 | 계획 | 실제 |
|---|---|---|---|
| 1 | trace 저장 | 설계 단계 | ✅ 완료 — **여기 쌓아둔 기록으로 나중에 결함 넷을 찾았다** |
| 2 | 진행 UI + 비용 대시보드 | MVP 포함 | ✅ 완료 |
| 3 | 자체 MCP 서버 | MVP 직후 | ✅ 완료 |
| 4 | n8n 스케줄 트리거 | MVP 완주 후 | ✅ 완료 (D-021) |
| 5 | 검토 에이전트 | 여유 시 | ✅ **완료** (D-020) — 미루지 않기로 바꿨다 |
| 6 | 카드 버전 롤백 | 여유 시 | ⬜ 남음 |

**5번을 앞당긴 이유** 카드 문구의 사실 오류를 사람 눈으로만 잡는 것이 불안했다.
실제로 검토자가 **원문에 없는 접종 기간을 지어낸 것**을 잡아냈다.
그때 넣지 않았으면 그 카드가 그대로 나갔을 것이다.

**1번의 값이 예상보다 컸다.** 계획할 때는 "평가 표를 만들려면 필요하다" 정도로 봤는데,
실제로는 **완주한 뒤에 쌓인 기록을 다시 읽다가 결함 넷을 찾았다** (D-022 · D-023 · D-024).
로그를 흘려보냈으면 넷 다 몰랐을 것이다.

---

## D-009 ✅ 캡처본을 구현 중에 그때그때 찍는다

**날짜** 2026-09-09

**배경** 제출 방식이 바뀌었다. **배포 주소를 내는 대신 저장소에 실행 결과 캡처를
넣는 것**으로 요구가 옮겨갔다.

**결정** 캡처를 마지막에 몰아 찍지 않고, **각 화면이 완성될 때마다 즉시** 찍어
`docs/screenshots/` 에 넣고 README에 삽입한다.

**이유**
1. **캡처본이 유일한 실행 증거가 됐다.** 보는 쪽이 직접 접속해 돌려보지 않으므로,
   캡처가 부실하면 구현을 증명할 방법이 없다
2. **실패·재시도 화면은 나중에 재현하기 어렵다.** 이미지 한도 초과나 도구 오류가
   마침 그때 일어나야 찍을 수 있다. 발생했을 때 바로 찍어둬야 한다

**주의** 제출 항목에서 빠졌다고 배포가 면제된 것이 아니다.
조건의 「배포」와 평가 문항 5번은 여전히 접속 가능한 URL로의 배포를 묻는다.
**배포는 하고, 그 화면을 캡처로 남긴다.**

---

## D-010 ✅ 두뇌 LLM — Gemini 2.5 Flash 기본 + OpenAI GPT-5 mini 비교·시연

**날짜** 2026-09-09

**전제 정리** 과제 자료 어디에도 **모델 지정이 없다.** 확인 결과:

| 문서 | 모델 언급 |
|---|---|
| 요구사항 | 프레임워크 예시만 언급. **모델은 "바꿔가며 비교할 세팅"으로만 등장** |
| 예시 자료 | 실행 엔진 종류만. 모델 미지정 |

조건문의 *"도구(API·MCP·검색·DB 등)"* 의 **API는 LLM API가 아니라 에이전트가 호출할 외부 도구**다.
두뇌는 문서가 언급조차 하지 않았고, **선택은 제출자의 자유**다.

**결정** 제공자를 어댑터로 감싸고 역할을 나눈다.

```
개발 · 디버깅 · 평가 세트 반복  →  Gemini 2.5 Flash   (무료 1,500회/일)
최종 품질 비교 · 시연 캡처       →  OpenAI GPT-5 mini  (기관 지급 크레딧 $5)
```

**조사한 수치**

| 후보 | 무료 한도 | 자동 과금 | 학습 사용 |
|---|---|---|---|
| Gemini 2.5 Flash | 1,500회/일, 카드 불필요 | 없음 (429만) | ⚠️ 있을 수 있음 |
| Gemini 2.5 Pro | **무료 티어에서 제거** (2026-04-01) | — | — |
| OpenAI GPT-5 mini | 기관 크레딧 $5 | 🔴 **계정 설정에 따라 초과 과금 가능** | 기본 미사용 |
| Claude Haiku 4.5 | 없음 ($1/$5) | 크레딧 소진 시 중단 | 미사용 |

**$5가 몇 회분인가** (1회 실행 추정: 입력 300K / 출력 30K 토큰)

| 모델 | 1회 비용 | $5로 |
|---|---|---|
| GPT-5 ($1.25/$10) | 약 $0.68 | 🔴 약 7회 |
| GPT-5 mini ($0.25/$2.00) | 약 $0.14 | 🟡 약 35회 |
| GPT-5 nano ($0.05/$0.40) | 약 $0.03 | 🟢 약 180회 |

**개발을 무료 모델로 하는 이유** $5는 개발 예산이 아니라 **시연 예산**이다.
평가 세트만 시나리오 5개 × 세팅 3개 = 15회이고, 개발 중 실패한 실행도 토큰을 먹는다.

**부수 효과** 두 제공자가 실제로 손에 있으니 **어느 쪽이 이 업무에 맞는지**를
억지 표가 아니라 실측으로 비교할 수 있다.

**필수 조건 — 함수 호출 지원 API여야 한다**
D-007에서 루프를 직접 구현하기로 했으므로 **내 앱이 도구를 정의**한다.
`claude -p` / `opencode run` 같은 **CLI 위임은 쓰지 않는다** — CLI 자체가 에이전트라
내가 정의한 도구 스키마와 실패 규칙이 사라진다.

**🔒 9번 규칙** Gemini 무료 티어는 입력이 학습에 쓰일 수 있다.
LINE 토큰·`.env` 내용·실서비스 자격증명은 **프롬프트에 넣지 않는다.**
이 프로젝트가 다루는 것은 공개 뉴스·날씨뿐이므로 위험은 낮으나, 필터를 코드에 둔다.

**비용 상한을 코드에 박는다** (PRD 3장)
Gemini: 1회 실행당 40회 / 일일 300회 · **OpenAI: 1회 실행당 $0.30, 누적 $3.00 도달 시 중단.**
기관 계정에 자동 과금 위험이 있어도 **앱이 먼저 멈춘다.**

---

## D-011 ✅ 검색은 Tavily, 원문 추출은 httpx + trafilatura

**날짜** 2026-09-09

| 후보 | 무료 한도 | 카드 | 판정 |
|---|---|---|---|
| **Tavily** | 1,000 크레딧/월 | 불필요 | ⭕ 채택 |
| Brave Search API | **2026년 2월 무료 티어 폐지.** $5 크레딧 후 과금 | 🔴 필수 | ❌ |
| NewsAPI.org | 개발자 플랜 무료 | 불필요 | ⚠️ **약관 확인 필요** — 개발 용도 한정·기사 지연 제약이 있어 배포 앱에 부적합할 수 있음. 폴백으로만 |
| Claude 내장 `web_search` | 검색당 과금 | — | 🟡 **스키마와 실패 처리를 내가 통제할 수 없다** |

**원문 추출은 벤더가 필요 없다** — `httpx`(가져오기) + `trafilatura`(본문 추출).
완전 무료·로컬이고 한국어 본문 추출이 양호하다.

**영향** 두 도구 모두 **입출력 스키마와 실패 규칙을 내가 정의**한다. 실패했을 때 어떻게 할지를 내가 정할 수 있다.

---

## D-012 ✅ 기술 스택과 개발·배포 기기

**날짜** 2026-09-09

**결정**

| 층 | 선택 |
|---|---|
| 웹 | FastAPI + Jinja2 + HTMX + SSE |
| 상태 정본 | SQLite (WAL 모드) |
| 제출 증거 | trace.json / outcome.json — SQLite에서 **내보낸** 파일 |
| 루프 실행 | asyncio 백그라운드 태스크 |
| 기기 | **노트북 Windows** |

**Streamlit 배제 이유** 스크립트 재실행 모델이라 5장의 *"새로고침 시 같은 질문 유지"*,
*"같은 답변 2번 제출 시 한 번만 실행"*, *"서버 재시작 후 상태 복원"* 을 구현할 수 없다.

**SQLite를 정본으로 두는 이유** 중복 차단이 `UNIQUE (run_id, question_id, version)` 한 줄이면 되고,
비용 대시보드 집계가 쿼리로 나온다. trace 파일은 PRD 7장이 요구하는 제출 증거라 없앨 수 없으므로,
**"정본 + 내보내기"** 로 정의해 두 곳이 어긋날 여지를 없앤다. WAL 모드는 SSE 읽기와 루프 쓰기가
동시에 일어나기 때문에 필요하다.

**Celery 등 별도 워커를 쓰지 않는 이유** 배포 단순성을 잃는다.
프로세스가 죽어도 상태 정본이 SQLite에 있으므로, 기동 시 `running` 실행을 찾아 이어가면 된다.

**기기 선택 근거**
- 저장소가 이미 Windows 경로에 있다. **WSL로 옮기면 3중 복제 위험** (15번 규칙)
- 제출 항목에서 배포 URL이 빠졌으므로 **상시 켜둘 기기가 필요 없다** (D-009).
  캡처를 남길 동안만 Cloudflare Tunnel을 띄운다
- 이 기기 확인 결과: Python 3.12.10 / Node 24 / uv / claude CLI / opencode 있음, **agy 없음**
- `.venv` 는 프로젝트 폴더 안. **폴더를 옮기면 절대경로가 깨진다**

---

## D-013 ✅ 카드 형식 — 손주 캐릭터가 안내한다. 캐릭터는 자산으로 재사용

**날짜** 2026-09-09

**결정** 카드뉴스는 **손자·손녀 캐릭터가 시니어에게 알려주는** 형식으로 만든다.
캐릭터는 매 카드마다 생성하지 않고 **1~2장만 만들어 자산으로 재사용**한다.
카드는 **배경 · 캐릭터 · 글자 3개 레이어**를 코드로 합성한다.

**왜 손주인가** 시니어는 자식 말보다 손주 말을 더 잘 듣는다.
전달력 문제이지 디자인 취향이 아니다 (온담이·이음이 운영 경험).
카드 문구의 어투도 여기 맞춘다 — *"할머니, 이건 꼭 기억하세요"*.

**왜 재사용인가** 이미지 생성 모델은 **여러 장에 걸쳐 같은 캐릭터를 유지하지 못한다.**
카드마다 얼굴이 달라지면 오히려 조악해 보인다.

| | 카드마다 생성 | **캐릭터 재사용** |
|---|---|---|
| 이미지 호출 | 5–8회 | **1–2회** |
| 캐릭터 일관성 | 🔴 깨짐 | ✅ 동일 |
| 한 장에 쏟을 수 있는 재시도 | 거의 없음 | **여러 번 다시 뽑을 수 있다** |
| 문구만 수정 | 이미지 재생성 필요 | 레이어만 교체 |

**품질 기준 — 쉬운 말, 전문가급 외형**
내용은 쉽게 풀되 **외형이 어설프면 신뢰가 떨어진다.** 초등학생 과제처럼 보이면
정보의 정확성과 무관하게 안 믿는다. 실제 챗봇 운영에서 겪은 문제다.

프로급 느낌은 그림이 아니라 **타이포그래피와 여백**에서 나온다. 따라서 코드 합성이 유리하다
(자간·행간·정렬을 매번 정확히 지킨다). 지킬 것:

- 고품질 한글 폰트 + **웨이트 대비** (제목 ExtraBold / 본문 Medium)
- 넉넉한 여백. 꽉 채우지 않는다
- **고정 그리드** — 5장이 같은 기준선을 쓴다
- 저채도 배경 + **강조색 1개**. 색을 늘리지 않는다
- 배경은 단색이 아니라 미묘한 그라데이션·기하 패턴

**영향**
- 이미지 생성 도구의 역할이 "카드 배경"에서 **"캐릭터 자산 제작"** 으로 바뀐다
- Antigravity 무료 한도 리스크가 크게 줄어든다 (D-003의 리스크 완화)
- 어투·금지 표현·글자 크기 하한은 MCP 도구 `get_audience_profile` 이 돌려줄 값으로 넣는다
- 참조 이미지는 **저작권 표기가 있는 자료라 저장소에 넣지 않는다.** 컨셉만 참고한다

---

## D-014 ✅ Gemini 모델 교체 · 이미지는 Antigravity CLI 로

**날짜** 2026-09-09

**배경** D-010 이 정한 `gemini-2.5-flash` 가 **신규 사용자에게 차단**됐다.
계정에 직접 모델 목록을 물어 확인했다.

```
404 NOT_FOUND: This model models/gemini-2.5-flash is no longer available to new users.
```

**결정 1 — 두뇌는 `gemini-3.6-flash`**
함수 호출까지 실물로 확인했다 (`get_weather(region="서울 노원구")` 반환).
`gemini-3.5-flash`, `gemini-3.1-flash-lite` 도 동작하므로 폴백 후보로 둔다.

**결정 2 — 이미지 생성은 Antigravity CLI**
Gemini 이미지 모델(`gemini-3.1-flash-image` 등)이 목록에는 있으나 **무료 티어 한도가 0** 이다.

```
quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier
limit: 0
```

결제를 붙여야만 쓸 수 있어서 제외했다 (전역 규칙 7번). **Antigravity CLI 가 무료로 쓸 수 있는
유일한 경로**다. 폴백은 2단으로 줄인다: ① Antigravity → ② 코드 생성 배경.

**부수 확인**
- `agy` 는 `~/.gemini/antigravity-cli/brain/<작업ID>/` 에 **JPEG** 로 만든 뒤 복사해 와야 한다
- `agy` 에게 후처리(파이썬)를 시키면 실패·재시도를 반복한다. **생성과 복사만 맡기고 가공은 우리 코드가 한다**
- 단가표에 없는 Gemini 모델에 보수적 단가가 붙어 무료 호출에 달러가 쌓였다.
  → `provider == "gemini"` 는 0 으로 처리하고 **호출 수**로 막는다

---

## D-015 ✅ 카드 디자인 시스템과 캐릭터 자산

**날짜** 2026-09-09

**결정** 카드는 배경·캐릭터·글자 3개 레이어를 코드로 조립한다. 디자인 시스템은
`app/cards/theme.py` 한 곳에 모은다.

**프로처럼 보이게 하는 것은 그림이 아니라 프레임이다**
참조 자료(시니어 카드뉴스 실물)를 분석해 뽑은 규칙:

| 요소 | 규칙 |
|---|---|
| 공통 프레임 | 5장이 같은 상단바·배경·여백을 쓴다. 그래서 세트로 읽힌다 |
| 색 | 남색 배경 + 흰 종이 + 강조 파랑. **표지만 골드 추가**. 색을 늘리지 않는다 |
| 위계 | 번호뱃지 → 제목(검정 + 강조어) → 본문 → 강조 박스 |
| 배경색 | **캐릭터 이미지에서 뽑아온 값.** 맞춘 게 아니라 가져왔다 |

**캐릭터 — 한 장에 여러 자세를 그린 시트로 만든다**
자세마다 따로 생성하면 얼굴이 조금씩 달라져 "다른 아이"로 보인다.
**한 렌더 안에서 나온 3자세**를 코드로 잘라 쓴다. 좌우 반전으로 4번째를 공짜로 얻는다.

```
pose_greet(인사) · pose_point(가리킴) · pose_open(양팔) · pose_point_left(반전)
```

**캐릭터가 가리키는 방향은 독자 시선을 끈다.** 오른쪽에 배치하면 **왼쪽(글 쪽)을 가리켜야** 한다.
바깥을 가리키면 시선이 카드 밖으로 나간다.

**배경 제거는 크로마키로 한다**
처음엔 남색 배경에 flood fill 을 썼는데 **검은 머리·청바지가 배경과 색이 이어져 구멍이 뚫렸다.**
→ 캐릭터에 없는 색(마젠타)을 배경으로 생성해 색 거리로 자른다. 배경색은 가정하지 않고
모서리에서 직접 잰다 (생성 모델은 지시한 색을 정확히 내지 않고, JPEG 압축까지 거친다).

**검사를 코드가 한다** (`app/cards/check.py`)
- 글자 크기 하한 · 배경 대비 명암비(WCAG AA 4.5:1) · 360px 축소본
- 실제로 **강조 파랑이 4.11:1 로 미달**인 것을 이 검사가 잡아냈다. 눈으로는 몰랐다
- 시니어 대상이라 "흐린 색 조합"은 사람 판단에 맡기지 않는다

**품질 기준** 쉬운 말 + 전문가급 외형. 내용이 맞아도 외형이 어설프면 신뢰가 떨어진다.

---

## D-016 ✅ 무료 티어 일일 한도 대응 — 모델 폴백 사슬

**날짜** 2026-09-09

**배경** 실행 중 갑자기 멈췄다. 원인을 파니 이랬다.

```
429 RESOURCE_EXHAUSTED
quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier
model: gemini-3.6-flash   quotaValue: 20
```

**무료 티어는 모델마다 하루 20회다.** `MAX_LLM_CALLS_PER_DAY=300` 으로 잡아둔 값이
현실과 전혀 맞지 않았다. 에이전트 한 번 돌리는 데 LLM 을 10회 넘게 부르므로
**하루 두 번이면 소진**된다.

**결정 — 한 모델이 막히면 다음 모델로 갈아탄다**

```
gemini-3.5-flash → 3.5-flash-lite → 3.1-flash-lite → 3-flash-preview → 3.6-flash
```

**한도가 모델마다 따로 걸린다**는 점을 이용한다. 실측으로 위 4개가 살아 있음을 확인했다.

**영향**
- 기본 모델을 `gemini-3.6-flash` → `gemini-3.5-flash` 로 바꿨다
- 사용량은 **실제로 응답한 모델**로 기록한다. 대체됐는데 원래 모델로 남기면 평가표가 틀어진다
- 429 는 예외로 죽지 않고 **중단**으로 남긴다. 진행분이 보존되고 사람이 다시 이어갈 수 있다
- 폴백이 있어야 한도에 걸려도 실행이 죽지 않는다

**남은 위험** 모든 모델이 소진되면 그날은 더 못 돌린다. 시연 전에 한도를 아껴 둘 것.

---

## D-017 ✅ 배포 — 전용 이름 터널을 따로 만든다

**날짜** 2026-09-09

**결정** `cloudflared` 로 `cardnews.dodami-ai.com` 에 붙인다.
**기존 서비스 설정(`~/.cloudflared/config.yml`)은 열지도 않고**, 전용 터널과 전용 config 를 새로 만든다.

**임시 터널을 버린 이유** `trycloudflare` 임시 터널을 먼저 시도했는데
호스트명 매핑이 붙지 않아 계속 404 였다. 연결도 1개만 등록됐다.
Cloudflare 자신이 로그에 *"no uptime guarantee"* 라고 밝히는 무보증 서비스다.
채점 기간에 살아 있어야 하므로 이름 있는 터널로 갔다.

**🔴 여기서 사고가 하나 났다 — 기록해 둔다**

```
cloudflared tunnel route dns cardnews cardnews.dodami-ai.com
→ Added CNAME ... tunnelID=fe3589b3-...   (이음이 터널)
```

`--config` 를 안 붙이면 **이름 인자를 무시하고 기본 설정의 터널로 CNAME 을 건다.**
`--config` 를 지정하고 `--overwrite-dns` 로 다시 걸어 바로잡았다.
이음이 ingress 규칙과 라우팅은 바뀌지 않았고 `chat.dodami-ai.com` 은 200 을 유지했다.

**교훈** 공용 CLI 가 여러 프로젝트의 설정을 공유할 때는
**모든 명령에 `--config` 를 명시**해야 한다. 전역 규칙 11번(기지에서 실행하지 않는다)의 CLI 판이다.

**수용 기준 재확인** README 의 표에 결과를 남겼다. 5개 항목 모두 통과.

---

## D-018 ✅ 카드 폰트는 Pretendard — 재배포 가능한 것으로

**날짜** 2026-09-09

**결정** 카드 글자에 Pretendard(SIL Open Font License 1.1) ExtraBold·Medium 두 굵기를 쓰고,
`assets/fonts/` 에 라이선스 전문과 함께 동봉한다.

**왜 바꿨나** 처음에는 Windows 기본 폰트인 맑은 고딕으로 만들었다. 구조를 잡는 데는
문제가 없었지만 **마이크로소프트 폰트는 재배포가 허용되지 않는다.** 저장소에 넣을 수 없고,
넣지 않으면 Windows 가 아닌 곳에서 폰트를 못 찾아 카드 합성이 실패한다.
제출물이 특정 컴퓨터에서만 도는 것은 맞지 않다.

**부수 효과** 글자 품질만 좋아진 게 아니라 **제목 줄바꿈 문제가 함께 풀렸다.**
맑은 고딕에서는 "생활 속에서 나타나는 초기 신호" 가 두 줄로 갈라지면서
강조어 "초기 신호" 가 줄 사이로 끊겼는데, Pretendard 가 조금 좁아서 한 줄에 들어간다.

**주의** 배포판 zip 의 `public/static/` 에는 **OTF** 만 있다.
TTF 는 `alternative/`(대체 글자꼴) 폴더에만 있으므로 표준 글자꼴을 쓰려면 OTF 를 써야 한다.
Pillow 는 OTF 를 문제없이 연다.

**폴백** `FONT_CANDIDATES` 는 Pretendard 를 먼저 찾고 없을 때만 맑은 고딕으로 떨어진다.
폰트가 아예 없는 환경을 위한 마지막 대비다.

---

## D-019 ✅ LINE 실채널 연결 — 잠금을 넷으로 늘리고 실발송까지 확인

**날짜** 2026-09-09

**결정** 전용 LINE 공식 계정(`시니어 카드뉴스` · `@916lzbqz`)을 새로 만들어 연결하고,
본인 한 명에게 실제로 보내 동작을 확인했다.

**왜 전용 계정인가** 온담이·이음이 채널을 재사용하면 실서비스 자격증명과 섞인다.
Provider 도 `카드뉴스 프로젝트` 를 따로 만들어 콘솔에서부터 갈라놨다.

**잠금을 셋에서 넷으로 늘렸다**

| # | 잠금 | 이유 |
|---|---|---|
| 1 | 기본값 dry-run | 아무것도 안 보내는 것이 기본 |
| 2 | `LINE_SEND_ENABLED` | 주소를 아는 사람이 눌러도 안 나가게 |
| 3 | 사람 승인 (`approved`) | 되돌릴 수 없는 작업 |
| 4 | **수신자 지정 (`LINE_TO`)** | **친구 전체 발송(broadcast)을 쓰지 않는다.** 나중에 친구가 늘면 사고가 된다 |

**보내기 전에 이미지가 밖에서 열리는지 먼저 확인한다**
LINE 은 이미지를 URL 로만 받는다. 그런데 **못 여는 URL 을 받아도 요청 자체는 200 을 돌려주고
사용자 화면에서만 조용히 깨진다.** "요청이 성공했다"와 "실제로 갔다"는 다르므로,
보내기 전에 직접 `GET` 해서 200 과 `content-type: image/*` 를 확인한다.

**배포가 선행 조건이었다** 로컬 파일은 못 보낸다. Cloudflare Tunnel 로 공개 주소가 생기면서
`{PUBLIC_BASE_URL}/output/<실행ID>/card_01.png` 를 넘길 수 있게 됐다 (D-017).

**확인 결과** 메시지 6건(안내 문구 1 + 카드 5장), 1.6초. 다섯 장 모두 깨짐 없이 표시됐다
(`docs/screenshots/10-line-sent.png`). 확인 직후 `LINE_SEND_ENABLED` 를 다시 `false` 로 되돌렸고,
승인해도 dry-run 으로 막히는 것까지 확인했다.
켜둔 채로 배포되어 있으면 접속한 사람이 버튼을 눌렀을 때 실제로 나갈 수 있다.

---

## D-020 ✅ 검토 에이전트 도입과 그 과정에서 드러난 세 가지 구멍

**날짜** 2026-09-09

**결정** 카드가 만들어지면 **다른 역할의 에이전트**가 문구를 검사한다.
만든 쪽에게 "잘 만들었니" 를 물으면 대체로 잘 만들었다고 답한다.
자기가 쓴 문장을 자기가 채점하는 셈이라,
*"작업을 끝냈다고 말하는 것과 실제 결과물이 만들어진 것은 다르다"* 를 못 잡는다.

검토자는 카드를 만들지 않는다. 시스템 프롬프트부터 다르다 — **트집만 잡는다.**
검사 항목은 다섯 가지다: 근거와 날짜·수치 일치 / 어려운 말 / 모호한 날짜 /
한 카드에 메시지 하나 / 단정적 의학 조언.

**실제로 잡아냈다**

```
[검토] 반려 3건
  카드4 [어려운말] "가까운 지정 의료기관을 미리 찾아보셔요"
  카드5 [어려운말] "주민등록증이나 운전면허증 지참"
  카드5 [메시지둘] 신분증 안내와 접종 시작일이 한 카드에 섞임
-> 스토리보드부터 재작업 -> 2차 검토 통과 -> 완주
```

반려가 무한히 이어지지 않도록 상한을 2회로 두고, 넘으면 사람에게 넘긴다.

---

### 붙이면서 드러난 구멍 세 가지

**① 단계별 도구 제한이 실효가 없었다**
`registry.specs(phase["tools"])` 로 모델에게 **보여주는 목록**만 걸렀는데,
모델이 목록에 없는 이름을 대면 루프가 전역 레지스트리에서 찾아 그대로 실행했다.
심층 검증 단계에서 `web_search` 를 6회 불러 반복 상한을 태웠다.
**권한 최소화는 목록을 안 주는 것으로는 부족하다.** 실행 직전에 허용 목록을 확인하게 고쳤다.

**② 모델이 검색어만 바꿔 무한히 검색했다**
조사 단계에서 `web_search` 를 12회 연속 호출했다. 자기가 몇 번 불렀는지 모르기 때문이다.
관찰 목록에 **도구별 호출 횟수**를 넣고, 3회를 넘으면 경고 문구를 붙였다.
단계 목표에도 "웹 검색은 최대 3회" 를 명시했다. 그 뒤로는 3회에서 멈췄다.

**③ 카드 본문에 줄바꿈 표기가 그대로 찍혔다**
모델이 본문에 줄바꿈을 문자 그대로 적어 넣었고, 렌더러가 그대로 그렸다.
카드에 백슬래시 n 이 보였다. `clean()` 을 넣어 wrap 전에 정리한다.

**반복 상한을 20 에서 30 으로 올렸다.**
검토자가 반려하면 스토리보드부터 다시 하므로 재작업 한 번에 5~6회가 더 든다.
20 이면 반려가 한 번만 나도 카드까지 못 간다. 상한이 고장난 게 아니라
**기준이 실제 동작과 안 맞았다.** 이런 조정은 근거를 남기지 않으면 그냥 봐주기가 된다.

**503 도 폴백 대상에 넣었다.** 429(한도 소진)만 처리했는데,
503(그 모델이 일시적으로 붐빔)에서 실행이 죽었다. 둘 다 "이 모델로는 지금 못 한다"는 뜻이다.

---

## D-021 ✅ 자동 트리거는 n8n 학원계정에 올린다

**날짜** 2026-09-10

**결정** 확장 4번(자동 트리거)을 n8n 으로 구현하고, **학원계정 인스턴스**에 올린다.
워크플로 JSON 은 `deploy/n8n/mq4-cardnews-schedule.json` 에 커밋한다.

**검토한 대안**

| 후보 | 판단 |
|---|---|
| **n8n 학원계정** ⭕ | 선택. 실서비스와 **완전히 분리**된다. 사고 반경이 0 |
| n8n 개인계정 | **온담이·이음이 실서비스가 여기 있다.** 같은 인스턴스에 과제 워크플로를 얹으면, 실습하다 잘못 건드릴 때 실서비스가 멈춘다 |
| 윈도우 작업 스케줄러 / APScheduler | 의존성이 없어 가장 단순하나, 평가 5번이 이름으로 부른 "자동 트리거"의 증거로는 약하다 |

**학원계정의 단점과 대응**
과정이 끝나면 계정이 사라질 수 있다. → **JSON 을 저장소에 커밋**해두면
계정 상태와 무관하게 *"자동 트리거를 구현했다"* 는 증거가 코드로 남는다.
채점 기간만 살아 있으면 되므로 실질적인 문제는 아니다.

**그래도 지킬 규칙** (인스턴스를 옮기게 되더라도)

1. 워크플로 이름 접두어 **`mq4-`** 유지
2. **자격증명을 새로 만들고** 기존 것을 재사용하지 않는다
3. 실서비스 워크플로는 **열지 않는다**

**검증 (2026-09-10)** 학원이 운영하는 자체 호스팅 n8n 인스턴스에 임포트하고
수동 실행했다. 세 노드 모두 통과했고, **데이터베이스에서 실제로 걸렸는지 대조**했다 —
*"응답이 왔다"* 와 *"제작이 시작됐다"* 는 다르다.

```
run-8c1ec54e1699   2026-09-09 16:28  (n8n 이 시작)
  단계1  list_past_publications → get_weather → web_search ×3 → finish_step
  단계2  ask_human  →  waiting_for_user
```

주제·지역이 워크플로 JSON 의 값과 일치하므로 화면에서 누른 실행이 아니다.
**사람이 시작하지 않았는데 조사가 끝나 있고, 결정은 사람이 기다려지는** 구조가 실물로 확인됐다.

**2026-09-10 확인** 온담이 때 쓴 n8n 은 Obsidian 일지에
`http://localhost:5678/webhook/report-schedule` 로 남아 있다 — **자체 호스팅**이었고
이 노트북이 아니라 데스크탑이었던 것으로 보인다. 이 노트북에는 5678 포트가 열려 있지 않고
n8n 이 설치돼 있지도 않다.

**앱을 수정하지 않은 이유**
`POST /runs` 가 이미 폼 데이터를 받는다. 화면에서 사람이 누르는 입구와
n8n 이 부르는 입구가 **같다.** 자동 경로용 API 를 따로 만들면 두 경로가 갈라져
한쪽이 시험되지 않은 채 남는다.

**설계 판단 — 리다이렉트를 따라가지 않는다**
`POST /runs` 는 `303` 으로 `/runs/<run_id>` 를 준다. 따라가면 HTML 만 받아
**어느 실행이 시작됐는지 알 수 없다.** `followRedirects: false` + `fullResponse: true` 로
두고 Location 헤더에서 `run_id` 를 꺼내, 실행 주소를 기록으로 남긴다.
HTTP 노드는 `neverError: true` 로 두고 **다음 노드에서 303 인지 직접 확인**한다 —
*"응답이 왔다"* 와 *"제작이 시작됐다"* 는 다르다.

**판단은 n8n 으로 옮기지 않는다** (D-006 유지). n8n 은 트리거만 담당한다.
노드로 조사·작성 순서를 짜면 그건 자동화이지 에이전트가 아니다.

---

## D-022 ✅ 검색 결과가 모델에 도달하지 않고 있었다

**날짜** 2026-09-10

**증상** 세 가지를 따로 기록해뒀는데 원인이 하나였다.

| 기록된 증상 | 어디에 적혀 있었나 |
|---|---|
| `fetch_article` 을 에이전트가 한 번도 스스로 고르지 않았다 | `CLAUDE.md` 약점 |
| 모델이 검색어만 바꿔 12회 반복했다 | `EVAL.md` 1장 |
| 원문을 직접 열어 날짜를 대조한 기록이 없다 | 요구사항 대조 |

**원인** `ToolResult.data` 가 **어디에서도 쓰이지 않았다.**
`web_search` 는 제목·URL·게시일·발췌를 모두 받아오는데, 저장되는 것은 `summary` 뿐이고
그 요약은 `검색어='...' 최근 7일 → 3건` 처럼 **건수만** 적고 있었다(약 50자).
`observations()` 는 `output_summary` 만 읽으므로, 다음 반복부터 모델은
**검색 결과를 본 적이 없는 상태**가 된다.

거기에 컨텍스트가 관찰 요약을 **300자로 자르고** 있었다. 링크가 실려 있었어도 잘렸을 것이다.

즉 `fetch_article` 을 고르지 않은 게 아니라 **열 대상을 몰랐다.**
검색을 반복한 것도 결과를 못 봐서였다. 호출 횟수를 알려준 조치는 원인이 아니라
증상을 억제한 것이었다.

**수정**

1. `search.py` — 요약에 `번호. 제목 (게시일) URL` 을 최대 8건까지 함께 싣는다
2. `loop.py` — 관찰 절단 길이를 도구별로 둔다 (`web_search` 1500 · `fetch_article` 900 · 나머지 300)
3. `state.py` — 단계 3 목표에 **도구 이름을 직접 적는다.**
   단계 1·4 에서 `list_past_publications` · `get_card_template` 을 이름으로 적었을 때
   모델 크기와 무관하게 불렸다(`EVAL.md` 2장). 단계 3 만 *"원문을 열어"* 라고만 적혀 있었다

**덤으로 고친 것 — 게시일 표기**
Tavily 는 RFC-2822(`Wed, 02 Sep 2026 ...`)를 주기도 한다. 앞 10자를 자르면
`Wed, 02 Se` 가 된다. `_day()` 로 `YYYY-MM-DD` 로 맞췄다.
**날짜 대조가 함정 1번**이라 표기를 흐리면 안 된다.

**검증** 검색 도구를 직접 불러 요약에 링크가 실리는 것과 날짜 정규화를 확인했다
(요약 48자 → 349자). 에이전트가 실제로 `fetch_article` 을 고르는지는 실행으로 확인한다.

---

## D-023 ✅ 단계 경계가 두 곳에서 새고 있었다 — 경고 누출과 게이트 누출

**날짜** 2026-09-10

D-022 로 검색 결과를 모델에 전달했는데도 `fetch_article` 이 여전히 안 불렸다.
원인을 더 파보니 **단계 경계를 넘어 새는 것이 두 개** 있었다.

### A) 호출 횟수 경고가 뒤 단계로 새어나갔다

호출 횟수는 **실행 전체**로 세고 있었다. 단계1 에서 `web_search` 를 3회 넘기면

> 경고: web_search 는 이미 3회 이상 불렀다. 같은 도구를 더 부르지 말고
> 지금 있는 자료로 판단해 finish_step 으로 넘어가라

이 경고가 **단계3 컨텍스트에도 그대로 실렸다.** 그래서 심층 검증 단계는
원문을 열기도 전에 `finish_step` 을 불렀다.

→ **단계 안에서만 센다** (`step_no == phase["no"]`). 경고 문구도 "이 단계에서" 로 바꿨다.

### B) 게이트가 앞 단계 답변으로 충족됐다 🔴

게이트 지시문이 이랬다.

> 아직 묻지 않았다면 ask_human 을 호출해 물어라.
> **이미 답을 받았다면** 그 답을 반영하고 finish_step 으로 넘어가라.

*"이미 답을 받았다면"* 이 **어느 질문인지 구분하지 않았다.** 그래서 단계1 에서 받은 답이
단계2(후보 선택) 게이트의 승인으로 쓰였다. 실측:

```
run-835a8c429601   단계2 [finish_step] 만        ← 질문 없이 건너뜀
                   단계4 [ask_human] 은 함        ← 같은 조건인데 물었다
```

**확정적으로 건너뛰는 게 아니라 비결정적으로 새는** 구조였다. 더 나쁘다 —
돌릴 때마다 사람 개입 지점 개수가 달라진다. 평가 항목이 *"승인 지점을 제공했는가"* 를
직접 묻는데 그 주장이 흔들리는 상태였다.

**고친 방법 — 지시문과 코드 둘 다**

1. `state.gate_state(run_id, step_no)` — **이 단계에서 물은 질문**만 찾는다.
   질문의 단계 번호는 `payload_json` 안에 이미 있어 스키마 변경이 필요 없었다
2. 게이트 지시문을 세 갈래로: 안 물었다 / 물었고 답 왔다 / 물었는데 답 없다
3. **`finish_step` 을 코드로 막는다.** 게이트인데 이 단계의 답이 없으면 거부하고
   재시도로 센다. 상한을 넘기면 사람에게 넘긴다

3번을 넣은 이유는 이 프로젝트가 이미 같은 교훈을 얻었기 때문이다 —
**"목록에 없는 도구도 이름만 대면 실행됐다" → 단계별 허용 목록을 코드로 강제** (D-020).
지시문만 믿으면 모델 판단에 맡겨진다.

### 검증 (run-ebec96b7eba9)

```
단계1  list_past_publications → web_search → get_weather → web_search ×2 → finish
단계2  ask_human → (사람 답변 2건) → finish        ← B 고쳐짐. 이전엔 finish 만
단계3  web_search FAIL(이 단계 불허)               ← 기존 방어도 작동
       fetch_article OK  원문 563자, 게시일 2026-09-04
       fetch_article OK  게시일 2026-08-16
       fetch_article OK  (재확인)
```

**`fetch_article` 이 실제로 원문을 열고 게시일을 확인한 첫 실행이다.**
반복 경고된 함정 *"검색 결과 요약만 보고 검증 완료 처리하지 말고 원문을 직접 열어
제목·날짜를 대조하라"* 의 증거가 이제 로그에 남는다.

**부수 효과** 코드 가드(3번)는 로그에 찍히지 않았다. 모델이 애초에 종료를 시도하지 않고
바로 물었다. 지시문으로 해결되고 **가드는 안전망으로 남은** 상태가 이상적이다.

---

## D-024 ✅ 반복 상한을 30 → 45 로 (두 번째 실측 조정)

**날짜** 2026-09-10

**결정** `max_loop_iterations` 를 45 로 올린다.

**근거 — 실행 5건**

| 실행 | 루프 | 완료 단계 | 결과 |
|---|---|---|---|
| `run-93d74c2888e7` | **27** | 7/7 | 완주 (상한 30의 **90%** 를 씀) |
| `run-ca32f9d1e18d` | 30 | 5/7 | 상한 도달 |
| `run-4605c934a58e` | 30 | 6/7 | 상한 도달 — **발송만 남기고** |
| `run-ce638ca65581` | 30 | 6/7 | 상한 도달 — **발송만 남기고** |

**유일한 완주 사례조차 여유가 3회뿐이었다.** 검토 반려가 한 번만 더 있었어도 못 갔다.
그 뒤 세 실행은 모두 6/7 또는 5/7 에서 멈췄다. 우연이 아니라 구조다.

**왜 30 으로는 모자라나**
7단계 각각이 최소 판단 1회 + 도구 호출 1~3회를 쓰고, 사람 개입 4곳이 각각 1회,
검토 반려 재작업 한 번에 5~6회가 든다. 다 더하면 30 을 넘긴다.
**무한 루프를 막는 장치가 정상 작업을 막고 있었다.**

**45 로 정한 이유** 완주 최소치(27)에 검토 반려 2회분(약 12회)과 여유를 더한 값이다.
무한 루프 방지라는 목적은 유지된다 — 45 도 유한하고, 시간·비용·호출 상한이 따로 걸려 있다.

**남겨둘 것** 상한을 올린 것으로 끝내지 않는다. 루프를 태우는 진짜 원인은 따로 있었고
그것들도 함께 고쳤다 — 질문 반복(D-023 후속, `max_asks_per_step`)과
발송 파일명 지어내기. 상한만 올리면 같은 문제가 45 에서 다시 난다.

---

## ⬜ 아직 정하지 않은 것

`PRD.md` 9장에 표로 정리돼 있다. 요약하면 다섯이다.

1. OpenAI 계정의 하드 리밋 확인 (앱은 누적 $3.00 에서 먼저 멈춘다)
2. 평가 지표 목표치 — 실행이 더 쌓여야 현실적인 값이 나온다
3. 카드 버전 롤백 (확장 6) — 확장 6개 중 유일하게 남은 것
4. 카드별 부분 재생성 — 지금은 반려 시 전체를 다시 합성한다
5. 테스트 코드
