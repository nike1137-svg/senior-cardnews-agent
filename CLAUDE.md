# 시니어 생활정보 카드뉴스 에이전트

> 이 파일은 **새 세션이 처음 읽는 인수인계서**다.
> 전역 규칙(`~/.claude/CLAUDE.md`)은 그대로 적용된다. 여기엔 이 프로젝트 고유 내용만 적는다.

---

## 이 프로젝트가 뭔가

**KDT 과제.** 주제 한 줄을 입력하면 AI 에이전트가 자료를 조사하고,
중간중간 사람에게 물어보며 진행해, 시니어용 카드뉴스 5장을 만들어주는 **웹 서비스**를
기획·구현·배포한다.

**핵심: 카드뉴스를 만드는 게 아니라, 카드뉴스를 만들어주는 서비스를 만드는 것.**

제출은 구글폼에 **GitHub 저장소 URL** 하나다 (2026-09-09 안내 변경 — 배포 URL 제출 항목이 삭제됨).

- 저장소: https://github.com/nike1137-svg/senior-cardnews-agent
- 제출 폼: https://forms.gle/ZHdKgApqf79YswNKA
- `PRD.md` 포함 ✅
- ✅ **실제 결과물 캡처본 포함** — `docs/screenshots/` **10장, 전부 README에 삽입 완료**

⚠️ **제출 항목에서 빠진 것과 채점에서 빠진 것은 다르다.**
조건의 「배포」 항목과 평가 문항 5번은 그대로 *"접속 가능한 URL로 배포"* 를 묻는다.
**배포는 하고, 그 화면을 캡처로 남긴다.** 캡처본이 곧 유일한 실행 증거다.

캡처 체크리스트는 `docs/screenshots/README.md` 에 있다. **구현하면서 그때그때 찍을 것.**
다 만들고 나서 찍으려면 실패·재시도 화면을 재현하기 어렵다.

🔴 **저장소가 현재 PRIVATE이다.** 제출 전에 반드시 공개로 바꿔야 채점자가 볼 수 있다.

공개 전환 전 점검 — 둘 다 **커밋 이력까지** 확인한다.

1. `.env`·API 키·토큰이 섞이지 않았는가
2. 🔴 **LMS 강의 자료·과제 안내 캡처가 들어 있지 않은가**
   2026-09-09에 이 문제로 저장소를 통째로 갈아엎었다 (이력 재작성 + 저장소 재생성).
   원본 사본은 `C:\Users\nike1\projects\notes\mainquest4-lms\` 에 있다. **다시 넣지 말 것.**

```bash
gh repo edit nike1137-svg/senior-cardnews-agent --visibility public --accept-visibility-change-consequences
```

---

## 🔴 제출 직전 체크리스트 (순서대로)

구현은 끝났다. 남은 건 **제출 절차뿐**이다. 하나라도 빠지면 채점이 안 된다.

- [ ] **1. 터널 켜기** — 채점자가 접속하려면 살아 있어야 한다

  ```bash
  cloudflared --config deploy/cloudflared.yml tunnel run cardnews
  ```
  같은 창에서 앱도 떠 있어야 한다: `uv run uvicorn app.main:app --port 8765`
  확인: <https://cardnews.dodami-ai.com/healthz> 가 `status: ok`

- [ ] **2. 저장소 공개 전환** — 지금 PRIVATE 이라 채점자가 못 본다

  ```bash
  gh repo edit nike1137-svg/senior-cardnews-agent --visibility public --accept-visibility-change-consequences
  ```
  공개 전 점검은 2026-09-09에 통과함 (`.env`·키·LMS 자료 이력에 없음).
  그 뒤 커밋을 더 했다면 다시 확인할 것.

- [ ] **3. 구글폼 제출** — <https://forms.gle/ZHdKgApqf79YswNKA>
  - GitHub 저장소 URL: `https://github.com/nike1137-svg/senior-cardnews-agent`

### 알아둘 것

- 앱에 **인증이 없다.** 주소를 아는 사람이 실행을 걸면 Gemini 무료 한도
  (모델당 하루 20회)를 소진시킬 수 있다. 채점 기간에만 켜두는 편이 안전하다
- LINE 발송은 dry-run 고정이라 채점자가 눌러도 실제로 나가지 않는다

---

## 문서 지도 — 어디에 뭐가 있나

| 파일 | 내용 | 우선순위 |
|---|---|---|
| `PRD.md` | 요구사항 명세. 워크플로·도구·개입지점·화면·평가설계 | 🔴 제일 먼저 읽기 |
| `DECISIONS.md` | 지금까지 내린 결정과 이유 (D-001~) | 🔴 그다음 |
| `EVAL.md` | 자동 생성. 지표·편차·실패 원인 표 | 🟡 |
| `docs/screenshots/README.md` | 제출용 캡처 10장 | 🟡 |

⚠️ **채점 기준(루브릭)과 강의 자료는 이 저장소에 두지 않는다.** LMS 원문이라 공개 저장소에
올릴 수 없다. 사본은 `C:\Users\nike1\projects\notes\mainquest4-lms\` 에 있고,
요구사항은 `PRD.md` 에 자체 언어로 재구성해뒀다. **충돌하면 루브릭이 이긴다.**

---

## 지금까지 진행 상황 (2026-09-10 기준)

**구현은 사실상 끝났다. 남은 것은 제출 절차와 선택 항목뿐이다.**

- [x] 기획 — `PRD.md` 11장 · `DECISIONS.md` **D-001~024**
- [x] 저장소 정리 — LMS 자료를 이력에서 제거하고 `senior-cardnews-agent` 로 이관
- [x] 프로젝트 뼈대 — FastAPI + SQLite(WAL), 테이블 6개
- [x] LLM 어댑터 — 모델 폴백 사슬(429·503), 비용·호출 상한, 키 필터
- [x] 도구 9종 — 스키마·설명·실패규칙·권한. **단계별 허용 목록을 코드로 강제**
- [x] 에이전트 루프 — 상태 저장·재개·**종료조건 7종** (재시도·반복·시간·이미지·반려·호출·비용)
- [x] 화면 — 시작 / 진행(SSE 로그·질문 카드) / 실행 기록 / 결과물
- [x] `trace.json` · `outcome.json` 내보내기 (병목 단계 자동 계산)
- [x] 자체 MCP 서버 ⭐확장3 — 도구 4종, 앱에 연결됨
- [x] 검토 에이전트 ⭐확장5 — 반려 → 재작업 → 통과까지 실물 확인
- [x] EVAL 자동 생성 + **제공자 2종·모델 4종을 3회씩 반복 측정** (편차 표 포함)
- [x] 배포 — https://cardnews.dodami-ai.com (D-017), 수용 기준 5개 통과
- [x] LINE 실채널 연결 — 잠금 4개, 실발송 확인 (D-019)
- [x] 폰트 Pretendard 동봉 (OFL, 재배포 가능)
- [x] 캡처 10장 (전부 README 삽입)
- [ ] **저장소 공개 전환** ← 제출 직전
- [x] **n8n 스케줄 트리거 ⭐확장4** — 학원 인스턴스(`n8n.vibemakers.kr`)에 임포트·수동 실행 완료.
  `run-8c1ec54e1699` 이 n8n 에서 시작돼 조사 후 `waiting_for_user` 로 멈춘 것까지 DB 대조함
- [ ] 카드 버전 롤백 ⭐확장6 (선택)

### 지표 (2026-09-10)

```
실행 35건 · 카드 생성 6건 · 완주(7/7) 2건 · 커밋 44개
```

완주율이 낮은 이유는 `EVAL.md` 1장에 원인 표(9줄)로 적어뒀다. **실패한 실행을 지우지 않았다.**
완주는 세 번 실패하고 네 번째에 됐고, 막힌 자리마다 원인이 달랐다 (D-023 · D-024).

### 이 기기 상태

| 있음 | `GEMINI_API_KEY` · `TAVILY_API_KEY` · LINE 토큰/수신자 · `agy` 1.1.28(로그인됨) · Pretendard |
|---|---|
| 없음 | — (OpenAI 키까지 채워져 제공자 비교가 가능하다. 실측 사용액 약 $0.05 / 크레딧 $5) |

`.env` 는 커밋되지 않는다. 값이 채워졌는지만 확인하려면:

```bash
uv run python -c "from app.config import get_settings as g; s=g(); print({k: bool(v) for k,v in [('gemini',s.gemini_api_key),('tavily',s.tavily_api_key),('line',s.line_channel_access_token)]})"
```

---

## 다음에 할 일

### 1순위 — 제출 (위 체크리스트 참고)

터널 켜기 → 저장소 공개 전환 → 구글폼 제출. 이것만 하면 제출은 끝난다.

### 2순위 — 남은 확장 (선택)

**확장 3·4·5는 모두 끝났다** (MCP 서버 · n8n 자동 트리거 · 검토 에이전트).
평가 5번이 이름으로 부른 셋이 3/3이다.

n8n 은 **학원 인스턴스**(`n8n.vibemakers.kr`)에 올렸다 (D-021).
🔴 워크플로가 **Published 상태면 월요일 06:00에 자동 실행**된다 — 한도를 아끼려면 Draft 로 내릴 것.

**카드 버전 롤백 ⭐확장6** — 재생성 전 카드를 남겨두고 되돌리기. 값이 가장 낮다. 남은 유일한 확장.

### 3순위 — 알려진 약점 (여유가 있으면)

- ~~`fetch_article` 이 실제 실행에서 한 번도 안 쓰였다~~ → **해결** (D-022 · D-023).
  원인이 셋이었다. ①검색 결과가 저장되지 않아 모델이 URL 을 본 적이 없었고
  ②관찰 요약이 300자로 잘려 링크가 사라졌고 ③단계1 의 "그만하고 finish_step 하라"
  경고가 단계3 까지 새어나가 원문을 열기 전에 종료됐다.
  `run-ebec96b7eba9` 에서 **원문 3건을 열고 게시일까지 확인**했다
- **카드별 부분 재생성이 없다.** 검토 반려 시 전체를 다시 합성한다.
  텍스트가 별도 레이어라 배경을 다시 만들지 않아 비용은 안 들지만,
  PRD 가 적어둔 *"승인된 카드는 유지"* 는 구현되지 않았다 (PRD 6장에 표시해둠)
- ~~평가 세트가 얇다~~ → **해결.** 같은 세팅을 3회씩 재고 제공자 축(OpenAI)을 붙였다.
  편차 표는 `EVAL.md` 2장에 있고 `scripts/compare_settings.py --repeat 3` 로 재생성한다
- **단계7에서 `record_publication` 을 부르지 않을 때가 있다.** 부르지 않으면 다음 실행이
  중복 주제를 못 걸러낸다. 단계 목표에 도구 이름을 적는 방법(D-022)을 여기에도 적용하면 된다
- **`send_line` 이 모델이 준 경로 개수를 그대로 쓴다.** 5장을 만들었는데 1장만 넘기면
  1장만 보낸다. 폴더에 있는 카드 수와 맞는지 확인하는 편이 낫다
- 테스트 코드가 없다

---

## 반드시 지킬 것

### 루브릭 때문에 (빠지면 감점)

- **에이전트 루프여야 한다.** 정해진 순서대로 흐르는 파이프라인은 감점.
  "도구 호출 → 결과 관찰 → **다음 행동 결정**" 이 반복돼야 한다
- **사람 개입 지점 4곳**을 반드시 남긴다 (소식 선택 / 스토리보드 승인 / 카드 검수 / 발송 승인)
- **실행 로그를 화면에** 띄운다. 도구명·호출 이유·결과·소요시간. 실행 종료 시 총 토큰·비용
- **종료 조건**을 둔다 (재시도 3회 / 루프 45회 / 작업시간 10분 / 이미지 8회 /
  검토 반려 2회 / 단계별 질문 3회 / 호출·비용 상한).
  작업시간은 **사람 대기를 뺀 값**이고, 루프는 20→30→45 로 두 번 실측해 올렸다 (D-020, D-024).
  🔴 **상한만 올리지 말 것.** 루프를 태우던 원인(질문 반복·파일명 지어내기)을 먼저 고쳤다
- **상태를 저장**한다. 서버가 재시작돼도 질문과 진행 상황이 복원돼야 한다
- 도구는 **2개 이상**, 각각 **입출력 스키마와 실패 처리 규칙**을 정의한다
- 🔴 **평가 5번은 확장을 이름으로 지목한다** — *"MCP 서버 제작·멀티 에이전트·자동 트리거 등"*.
  trace 저장·비용 대시보드는 **평가 4번의 필수 요소**이지 5번 가점이 아니다.
  5번을 채우려면 **자체 MCP 서버**가 사실상 최소 요건이다

### 안전 때문에 (사고 방지)

- 🔴 **LINE 발송은 기본 `dry-run`.** 실발송은 ①사람 승인 ②환경변수 스위치 둘 다 필요.
  채점자가 눌러도 시니어에게 나가면 안 된다
- 🔴 **발송 실패 시 자동 재시도 금지** (중복 발송 위험). 사람이 다시 누르게 한다
- 🔴 **Antigravity CLI에 `--dangerously-skip-permissions` 쓰지 않는다.** 필요한 도구만 개별 허용
- 🔴 **API 키는 환경변수.** `.env` 커밋 금지
- 🔴 **OpenAI 비용 상한을 코드에 박는다** — 1회 실행당 $0.30, 누적 $3.00 도달 시 중단.
  기관 지급 크레딧 $5이고, **계정에 결제수단이 연결돼 있으면 초과분이 기관에 청구된다.**
  대시보드 Usage limits 확인은 별개로 해야 한다
- 🔴 **Gemini 무료 티어는 입력이 학습에 쓰일 수 있다** (9번 규칙).
  LINE 토큰·`.env` 내용·실서비스 자격증명을 **프롬프트에 넣지 않는다.**
  도구 결과를 프롬프트에 담을 때 키가 섞이지 않는지 걸러내는 필터를 둘 것
- 🔴 **CLI 위임(`claude -p`, `opencode run`)을 쓰지 않는다** — CLI 자체가 에이전트라
  내가 정의한 도구 스키마가 남지 않고 루브릭 2번 증거가 약해진다. **함수 호출 API로 직접 호출**
- 🟡 파일 쓰기는 `output/<실행ID>/` 안으로만 제한
- 🟡 온담이/이음이 등 **실서비스 n8n 워크플로와 절대 섞지 않는다.** 인스턴스를 공유하더라도
  워크플로와 자격증명은 분리

### 함정 (반복해서 지적된 것)

1. **검색 결과 요약만 보고 검증 완료 처리 ❌** → 원문 링크를 직접 열어 제목·날짜 대조
2. **"이미지 요청을 보냈다" ≠ "이미지 파일이 생겼다"** → 파일이 실제로 열리는지 확인 후 완료 표시
3. **"종료 코드 0" ≠ "성공"** → headless는 권한이 거부돼도 응답만 오는 경우가 있다

---

## 알려진 제약

| 항목 | 내용 |
|---|---|
| **Antigravity 이미지** | 캐릭터 자산 제작에만 썼다(4자세). 카드 배경은 코드로 그리므로 실행할 때마다 이미지 API 를 부르지 않는다. Gemini 이미지 API 는 무료 한도가 0 이라 못 쓴다 (D-014) |
| **글자는 별도 레이어** | 이미지 모델이 그린 글자는 오타가 나고 수정이 안 된다. 배경(1024×1024)만 생성하고 텍스트는 코드로 합성(최종 1080×1350) |
| **시니어 가독성** | 글자 크기 하한, 카드당 메시지 1개, 외래어 금지, 날짜는 "9월 15일 월요일" 식으로 명시 |
| **폰트** | Pretendard(SIL OFL) 동봉 완료. 맑은 고딕은 재배포 불가라 폴백으로만 남겼다 (D-018) |

---

## 마커스님 관련 메모

- **n8n 실사용 경험 있음** (온담이 프로젝트). 새로 배울 필요 없음
- 자체 호스팅 인프라 보유 (Cloudflare Tunnel, nginx, systemd) → 배포에 유리
- MCP 서버 제작 경험 있음 (`projects/study/mcp-practice`) → 루브릭 5번 가점 후보
- 설명할 때는 **단계별로**, 명령어에는 **환경 표시(🐧/🪟/🔴)** 를 붙일 것 (전역 규칙)
