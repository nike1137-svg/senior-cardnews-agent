# 시니어 생활정보 카드뉴스 에이전트

> 이 파일은 **새 세션이 처음 읽는 인수인계서**다.
> 전역 규칙(`~/.claude/CLAUDE.md`)은 그대로 적용된다. 여기엔 이 프로젝트 고유 내용만 적는다.

---

## 이 프로젝트가 뭔가

**KDT 과제.** 주제 한 줄을 입력하면 AI 에이전트가 자료를 조사하고,
중간중간 사람에게 물어보며 진행해, 시니어용 카드뉴스 5장을 만들어주는 **웹 서비스**를
기획·구현·배포한다.

**핵심: 카드뉴스를 만드는 게 아니라, 카드뉴스를 만들어주는 서비스를 만드는 것.**

제출은 구글폼에 **GitHub 저장소 URL** 하나다 (2026-09-09 안내 변경 — 배포 URL 제출 항목이 삭제됨).

- 저장소: https://github.com/nike1137-svg/senior-cardnews-agent
- 제출 폼: https://forms.gle/ZHdKgApqf79YswNKA
- `PRD.md` 포함 ✅
- 🔴 **실제 결과물 캡처본 포함** — `docs/screenshots/` 에 넣고 README에 삽입. **아직 없음**

⚠️ **제출 항목에서 빠진 것과 채점에서 빠진 것은 다르다.**
조건의 「배포」 항목과 평가 문항 5번은 그대로 *"접속 가능한 URL로 배포"* 를 묻는다.
**배포는 하고, 그 화면을 캡처로 남긴다.** 캡처본이 곧 유일한 실행 증거다.

캡처 체크리스트는 `docs/screenshots/README.md` 에 있다. **구현하면서 그때그때 찍을 것.**
다 만들고 나서 찍으려면 실패·재시도 화면을 재현하기 어렵다.

🔴 **저장소가 현재 PRIVATE이다.** 제출 전에 반드시 공개로 바꿔야 채점자가 볼 수 있다.

공개 전환 전 점검 — 둘 다 **커밋 이력까지** 확인한다.

1. `.env`·API 키·토큰이 섞이지 않았는가
2. 🔴 **LMS 강의 자료·과제 안내 캡처가 들어 있지 않은가**
   2026-09-09에 이 문제로 저장소를 통째로 갈아엎었다 (이력 재작성 + 저장소 재생성).
   원본 사본은 `C:\Users\nike1\projects\notes\mainquest4-lms\` 에 있다. **다시 넣지 말 것.**

```bash
gh repo edit nike1137-svg/senior-cardnews-agent --visibility public --accept-visibility-change-consequences
```

---

## 문서 지도 — 어디에 뭐가 있나

| 파일 | 내용 | 우선순위 |
|---|---|---|
| `PRD.md` | 요구사항 명세. 워크플로·도구·개입지점·화면·평가설계 | 🔴 제일 먼저 읽기 |
| `DECISIONS.md` | 지금까지 내린 결정과 이유 (D-001~) | 🔴 그다음 |
| `docs/screenshots/README.md` | 제출용 캡처 체크리스트 8종 | 🟡 |

⚠️ **채점 기준(루브릭)과 강의 자료는 이 저장소에 두지 않는다.** LMS 원문이라 공개 저장소에
올릴 수 없다. 사본은 `C:\Users\nike1\projects\notes\mainquest4-lms\` 에 있고,
요구사항은 `PRD.md` 에 자체 언어로 재구성해뒀다. **충돌하면 루브릭이 이긴다.**

---

## 지금까지 진행 상황 (2026-09-09 기준)

- [x] 폴더·git 초기화
- [x] 루브릭 분석, 강의 자료와 대조
- [x] 도메인 확정 — 시니어 생활정보 안내
- [x] 출력 형태 확정 — **카드뉴스 이미지 (A안)**
- [x] `PRD.md` 작성 (루브릭 필수 6항목 + 종료조건 + 실패처리 + 평가설계)
- [x] 확장 계획 확정 (`PRD.md` 10장) — 원문 불릿 5개(낱개 7개) 검토, 1~4번 구현 약속
- [x] **LMS 자료 저장소에서 제거** — 이력 재작성 + 저장소 재생성 (2026-09-09)
- [x] GitHub 저장소 생성 (PRIVATE)
- [x] **두뇌 LLM 확정** (D-010 → **D-014로 교체**) — `gemini-3.6-flash`
- [x] **검색·원문 도구 확정** (D-011) — Tavily + httpx/trafilatura
- [x] **기술 스택·기기 확정** (D-012) — FastAPI+Jinja2+HTMX+SSE / SQLite(WAL) / 노트북 Windows
- [x] **카드 형식·캐릭터 확정** (D-013 · D-015) — 손주 캐릭터, 자세 4종 자산 완성
- [x] **프로젝트 뼈대** — FastAPI + SQLite(WAL), 테이블 5개, `/healthz` 통과
- [x] **LLM 어댑터** — Gemini/OpenAI 교체 가능, 상한 카운터, 키 필터. **실물 함수호출 검증**
- [x] **카드 합성** — 배경·캐릭터·글자 3레이어, 명암비/글자크기 자동 검사
- [x] **도구 5종** — 스키마·description·실패처리·권한. 실패 주입 스위치
- [x] **에이전트 루프** — 실제로 돌아 도구 호출 → 실패 → 사람에게 질문 → 대기 저장까지 확인
- [x] **질문/재개 화면 + SSE 실행 로그 패널** — 재개·중복차단·stale 전부 실물 검증
- [x] **`trace.json` · `outcome.json` 내보내기** — 병목 단계까지 자동 계산
- [x] **결과물 확인** — 카드 미리보기 + ZIP 내려받기
- [x] **자체 MCP 서버** (⭐확장3) — 도구 4종, 앱에 연결됨
- [x] **EVAL.md 자동 생성** — 같은 조건에서 모델만 바꾼 비교 표
- [x] **배포** — https://cardnews.dodami-ai.com (D-017), 수용 기준 5개 통과
- [ ] 저장소 공개 전환 (제출 직전)
- [ ] (여유 시) n8n 스케줄 트리거 ⭐확장4 · 검토 에이전트 ⭐확장5

### 이 기기에 있는 것 / 없는 것 (2026-09-09 기준)

| 있음 | Python 3.12·3.13 · Node 24 · uv · claude CLI · opencode · **`agy` 1.1.28 (로그인 완료)** · `GEMINI_API_KEY` |
|---|---|
| **없음** | 🔴 **Tavily 키** — 없으면 `web_search` 가 실패하고 조사 단계가 사람 질문으로 넘어간다 |
| | `OPENAI_API_KEY` (VS Code에만 있음. 없어도 Gemini 로 전부 동작) |

### 🔴 다음 세션이 가장 먼저 알아야 할 것

- **Gemini 이미지 API 는 무료 한도가 0 이다.** 이미지 생성은 `agy` 로만 한다 (D-014)
- **`agy` 에게 후처리를 시키지 마라.** 생성·복사만 시키고 가공은 우리 코드가 한다.
  파이썬 후처리를 시키면 실패·재시도를 무한 반복한다
- **Open-Meteo 지오코딩은 한국 지명에 틀린 답을 준다.** `app/tools/regions.py` 내장 표를 쓴다
- 실패 화면 캡처가 필요하면 `FAULT_INJECT=search_empty,image_fail` 로 언제든 재현한다

---

## 다음에 할 일 (순서대로)

1~4번(뼈대 · 어댑터 · 도구 · 루프)은 **완료**. 여기서부터다.

1. **질문/재개 화면 + 실행 로그 패널(SSE)** ← 평가 4번. 다음에 할 것
   - 화면1 시작 → 화면2 진행(질문 카드 + 로그) → 화면3 검수 → 화면4 결과 → 화면5 실행기록
   - 재개 검증 5종을 반드시 통과시킬 것 (PRD 5장 표)
2. **`runs/<실행ID>/trace.json` · `outcome.json` 내보내기** ⭐확장1
   - 원본은 이미 `tool_calls` · `llm_usage` 에 쌓이고 있다. 내보내기만 붙이면 된다
3. 검수·내보내기 (PNG·ZIP·출처기록)
4. 비용·소요시간 대시보드 화면 ⭐확장2
5. 실험·평가 표 (`EVAL.md`) — 도구 description·프롬프트를 바꿔가며 비교
6. 배포 + **수용 기준 재확인 기록**
   ───── 여기까지 MVP ─────
7. 자체 MCP 서버 ⭐확장3 ← 🔴 **평가 5번이 이름으로 지목한 항목. 빼면 한 문항이 빈다**
8. n8n 스케줄 트리거 ⭐확장4
9. (여유 시) 검토 에이전트 ⭐확장5 · 카드 버전 롤백 ⭐확장6

**각 단계에서 화면이 완성되면 그때그때 캡처를 찍어 `docs/screenshots/` 에 넣는다.**

---

## 반드시 지킬 것

### 루브릭 때문에 (빠지면 감점)

- **에이전트 루프여야 한다.** 정해진 순서대로 흐르는 파이프라인은 감점.
  "도구 호출 → 결과 관찰 → **다음 행동 결정**" 이 반복돼야 한다
- **사람 개입 지점 4곳**을 반드시 남긴다 (소식 선택 / 스토리보드 승인 / 카드 검수 / 발송 승인)
- **실행 로그를 화면에** 띄운다. 도구명·호출 이유·결과·소요시간. 실행 종료 시 총 토큰·비용
- **종료 조건**을 둔다 (재시도 3회 / 루프 20회 / 10분 / 이미지 8회)
- **상태를 저장**한다. 서버가 재시작돼도 질문과 진행 상황이 복원돼야 한다
- 도구는 **2개 이상**, 각각 **입출력 스키마와 실패 처리 규칙**을 정의한다
- 🔴 **평가 5번은 확장을 이름으로 지목한다** — *"MCP 서버 제작·멀티 에이전트·자동 트리거 등"*.
  trace 저장·비용 대시보드는 **평가 4번의 필수 요소**이지 5번 가점이 아니다.
  5번을 채우려면 **자체 MCP 서버**가 사실상 최소 요건이다

### 안전 때문에 (사고 방지)

- 🔴 **LINE 발송은 기본 `dry-run`.** 실발송은 ①사람 승인 ②환경변수 스위치 둘 다 필요.
  채점자가 눌러도 시니어에게 나가면 안 된다
- 🔴 **발송 실패 시 자동 재시도 금지** (중복 발송 위험). 사람이 다시 누르게 한다
- 🔴 **Antigravity CLI에 `--dangerously-skip-permissions` 쓰지 않는다.** 필요한 도구만 개별 허용
- 🔴 **API 키는 환경변수.** `.env` 커밋 금지
- 🔴 **OpenAI 비용 상한을 코드에 박는다** — 1회 실행당 $0.30, 누적 $3.00 도달 시 중단.
  기관 지급 크레딧 $5이고, **계정에 결제수단이 연결돼 있으면 초과분이 기관에 청구된다.**
  대시보드 Usage limits 확인은 별개로 해야 한다
- 🔴 **Gemini 무료 티어는 입력이 학습에 쓰일 수 있다** (9번 규칙).
  LINE 토큰·`.env` 내용·실서비스 자격증명을 **프롬프트에 넣지 않는다.**
  도구 결과를 프롬프트에 담을 때 키가 섞이지 않는지 걸러내는 필터를 둘 것
- 🔴 **CLI 위임(`claude -p`, `opencode run`)을 쓰지 않는다** — CLI 자체가 에이전트라
  내가 정의한 도구 스키마가 남지 않고 루브릭 2번 증거가 약해진다. **함수 호출 API로 직접 호출**
- 🟡 파일 쓰기는 `output/<실행ID>/` 안으로만 제한
- 🟡 온담이/이음이 등 **실서비스 n8n 워크플로와 절대 섞지 않는다.** 인스턴스를 공유하더라도
  워크플로와 자격증명은 분리

### 함정 (반복해서 지적된 것)

1. **검색 결과 요약만 보고 검증 완료 처리 ❌** → 원문 링크를 직접 열어 제목·날짜 대조
2. **"이미지 요청을 보냈다" ≠ "이미지 파일이 생겼다"** → 파일이 실제로 열리는지 확인 후 완료 표시
3. **"종료 코드 0" ≠ "성공"** → headless는 권한이 거부돼도 응답만 오는 경우가 있다

---

## 알려진 제약

| 항목 | 내용 |
|---|---|
| **Antigravity 이미지 한도** | 무료 티어가 계속 축소돼 왔다(5시간 롤링 리셋). 카드 5장 + 재생성이면 금방 닿는다. → **실패 시 단색/패턴 배경으로 대체해 카드는 반드시 완성**시킨다 |
| **글자는 별도 레이어** | 이미지 모델이 그린 글자는 오타가 나고 수정이 안 된다. 배경(1024×1024)만 생성하고 텍스트는 코드로 합성(최종 1080×1350) |
| **시니어 가독성** | 글자 크기 하한, 카드당 메시지 1개, 외래어 금지, 날짜는 "9월 15일 월요일" 식으로 명시 |
| **폰트 라이선스** | 상업적 이용 가능 여부 확인 후 사용 |

---

## 마커스님 관련 메모

- **n8n 실사용 경험 있음** (온담이 프로젝트). 새로 배울 필요 없음
- 자체 호스팅 인프라 보유 (Cloudflare Tunnel, nginx, systemd) → 배포에 유리
- MCP 서버 제작 경험 있음 (`projects/study/mcp-practice`) → 루브릭 5번 가점 후보
- 설명할 때는 **단계별로**, 명령어에는 **환경 표시(🐧/🪟/🔴)** 를 붙일 것 (전역 규칙)
